// Stub for `import "../global.css"` under Jest — NativeWind v5 compiles the CSS
// through Metro, which Jest doesn't run. Tests assert behavior (text / role /
// testID), not styles, so an empty module is sufficient.
module.exports = {};
