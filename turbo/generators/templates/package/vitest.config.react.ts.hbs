import { reactConfig } from "@repo/vitest-config/react";

// jsdom + React component suite — the shared react base (plugin-react, dedupe,
// jest-dom/cleanup setup) is the whole config (ADR 0025).
export default reactConfig;
