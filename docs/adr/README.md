# Architecture Decision Records

Each ADR captures one decision: the context, the decision, why, and the
consequences. To change a decision, supersede it with a revised ADR.

Format: Status / Context / Decision / Consequences / Sources.

"Status" is the ADR's decision status (all Accepted). "Implementation" tracks
the as-built reality: **Implemented** (shipped), **Partial** (core shipped, a
device/external-account-gated step remains), or **Seam** (decided and the seam
exists, deliberately not wired — by design).

| ADR                                                        | Title                                                                                               | Status   | Implementation                                  |
| ---------------------------------------------------------- | --------------------------------------------------------------------------------------------------- | -------- | ----------------------------------------------- |
| [0001](0001-styling-split-ui-tailwind-v4.md)               | Split UI: web Tailwind v4 + shadcn, mobile NativeWind v5; shared token theme                        | Accepted | Partial — device smoke-test pending             |
| [0002](0002-reanimated-4-retained.md)                      | Retain Reanimated 4 (no NativeWind conflict)                                                        | Accepted | Partial — installed; device run pending         |
| [0003](0003-cross-platform-navigation.md)                  | Per-platform routing + in-repo route contract (no Solito)                                           | Accepted | Implemented                                     |
| [0004](0004-theming-token-system.md)                       | Shared Tailwind v4 `@theme` token system                                                            | Accepted | Implemented                                     |
| [0005](0005-testing-two-runner-split.md)                   | Vitest (web + shared) / Jest (mobile); Maestro E2E                                                  | Accepted | Partial — Maestro/EAS activation pending        |
| [0006](0006-split-ui-web-dom-mobile-rn.md)                 | Split UI: web DOM/RSC, mobile RN; share logic not pixels                                            | Accepted | Implemented                                     |
| [0007](0007-rest-data-layer.md)                            | REST data layer: stable `@repo/api` barrel, OpenAPI-or-hand-written behind it                       | Accepted | Implemented                                     |
| [0008](0008-shared-package-boundaries.md)                  | Shared package boundaries: separate api / validators / utils / config; retire `@repo/shared`        | Accepted | Implemented                                     |
| [0009](0009-forms-rhf-zod-no-package.md)                   | Forms: React Hook Form + zod, schemas in `@repo/validators`, no `@repo/forms` package               | Accepted | Implemented                                     |
| [0010](0010-ui-state-zustand-store-package.md)             | UI state: Zustand in a `@repo/store` package; theme as the first store                              | Accepted | Implemented                                     |
| [0011](0011-enforce-package-boundaries-with-eslint.md)     | Enforce the ADR 0008 dependency DAG with `eslint-plugin-boundaries`                                 | Accepted | Implemented                                     |
| [0012](0012-api-client-factory.md)                         | API client as an explicit factory (no module-global transport); middleware + AbortSignal            | Accepted | Implemented                                     |
| [0013](0013-expo-sdk-56-upgrade.md)                        | Upgrade to Expo SDK 56 (RN 0.85 + React 19.2 + TypeScript 6.0); supersedes ADR 0002 pins            | Accepted | Implemented                                     |
| [0014](0014-error-handling-exceptions-at-the-data-seam.md) | Error handling: exceptions at the data-layer seam; no `Result` idiom                                | Accepted | Implemented                                     |
| [0015](0015-mobile-storage-asyncstorage-over-mmkv.md)      | Mobile key-value storage: AsyncStorage over MMKV — keep the app Expo Go-compatible                  | Accepted | Implemented                                     |
| [0016](0016-auth-jwt-refresh-package.md)                   | Auth: JWT + refresh-token rotation in a `@repo/auth` package; refresh as injected middleware        | Accepted | Partial — web wired; mobile adapter deferred    |
| [0017](0017-auth-client-hardening.md)                      | Auth client hardening: cookie access token, cross-tab refresh lock, server-side gating              | Accepted | Implemented (web)                               |
| [0018](0018-bff-route-handler-and-shared-mocks.md)         | Same-origin BFF route handler + shared framework-agnostic mocks (`@repo/api-mocks`)                 | Accepted | Implemented                                     |
| [0019](0019-openapi-codegen-seam.md)                       | OpenAPI codegen seam: hand-written zod kept swap-ready for `@hey-api/openapi-ts` (documented)       | Accepted | Seam — documented, not wired (by design)        |
| [0020](0020-i18n-next-intl-use-intl.md)                    | i18n: `@repo/i18n` with next-intl (web) + use-intl (mobile), shared ICU catalogs                    | Accepted | Implemented                                     |
| [0021](0021-telemetry-observability-package.md)            | Telemetry: `@repo/telemetry` — Sentry errors/perf + logging sink + agnostic analytics seam          | Accepted | Partial — native Sentry capture deferred        |
| [0022](0022-typed-search-params-route-dx.md)               | Route DX: zod-typed search params + active-route helpers in `@repo/navigation` (`navigation → zod`) | Accepted | Implemented                                     |
| [0023](0023-datetime-intl-temporal-deferred.md)            | Date/time: Intl-based formatting; no date-math lib; Temporal deferred behind a lib-agnostic seam    | Accepted | Seam — Temporal deferred (by design)            |
| [0024](0024-scaffolding-generators-turbo-gen.md)           | Scaffolding generators (`@turbo/gen`): auto-wired `package` / `api-resource` / `route` generators   | Accepted | Implemented                                     |
| [0025](0025-web-e2e-playwright-shared-vitest-config.md)    | Web E2E (Playwright, mock-mode) + shared `tooling/vitest-config`; no `@repo/testing` grab-bag       | Accepted | Implemented                                     |
| [0026](0026-web-security-headers-csp.md)                   | Web security headers + nonce-based CSP (standalone deploy)                                          | Accepted | Implemented                                     |
| [0027](0027-toast-notification-store.md)                   | Toast/notification queue as the second `@repo/store` store (custom Toaster, pure queue)             | Accepted | Implemented                                     |
| [0028](0028-feature-flags-posthog.md)                      | Feature flags: `@repo/flags` seam + PostHog adapter (RSC bootstrap); shared client with telemetry   | Accepted | Implemented (web + mobile)                      |
| [0029](0029-realtime-package-centrifugo.md)                | Realtime: `@repo/realtime` seam + Centrifugo adapter (recovery-aware, mock + no-op defaults)        | Accepted | Seam — consumer-ready, no app wired (by design) |
| [0030](0030-api-response-envelope-seam.md)                 | API response-envelope seam in `createApiClient` (`unwrap` + `mapError`; amends 0007/0012)           | Accepted | Implemented                                     |
| [0031](0031-nestjs-modular-monolith-worker-split.md)       | Backend: NestJS 11 on Fastify — modular monolith, api/worker/migrate deployables from one image     | Accepted | Implemented (shell)                             |
| [0032](0032-postgres-drizzle-db-package.md)                | PostgreSQL + Drizzle 1.0-rc in `@repo/db` (built pkg, per-module schema, pii() registry, factories) | Accepted | Implemented (foundation)                        |
| [0033](0033-better-auth.md)                                | Auth: Better Auth cookie sessions (manual Fastify mount, Drizzle+Redis, admin on, org dormant)      | Accepted | Implemented (supersedes 0016/0017 client)       |
| [0034](0034-api-contract-and-seams.md)                     | API contract: shared zod = wire contract (records 0039); thin seams — webhooks, billing, AI, keys   | Accepted | Implemented (contract record); seams by design  |
| [0035](0035-infra-modules.md)                              | Infra modules: locale-aware email seam, S3 presign, Centrifugo v6 (closes 0029), 2-layer throttling | Accepted | Implemented (live-probed)                       |
| [0036](0036-backend-observability.md)                      | Backend observability: OTel (outbox trace continuity, gauges) + Sentry (PII-scrubbed) + PostHog BE  | Accepted | Implemented (live-probed)                       |
| [0037](0037-transactions-outbox.md)                        | Ambient @Transactional() (CLS) + transactional outbox (SKIP LOCKED relay, jobId dedup, IDs-only)    | Accepted | Implemented (concurrency-proven)                |
| [0043](0043-jobs-scheduling.md)                            | Jobs: BullMQ conventions, repeatables-only cron (@nestjs/schedule banned), DLQ + bull-board         | Accepted | Implemented                                     |
| [0038](0038-zero-downtime-migrations-pooling.md)           | Zero-downtime migrations (release-phase one-shot, expand/contract) + pooling doctrine               | Accepted | Doctrine (gates live with first migration)      |
| [0039](0039-api-semantics.md)                              | API semantics: keyset pagination, opt-in idempotency replay, mandatory zod response serialization   | Accepted | Implemented (live-probed)                       |
| [0040](0040-gdpr-privacy-audit.md)                         | GDPR plumbing: privacy export/erase fan-out, append-only audit, pii() registry, retention jobs      | Accepted | Implemented (live-probed)                       |
| [0041](0041-tenancy-seam.md)                               | Tenancy seam: scoped repositories + dormant org data model; 5-step retrofit playbook                | Accepted | Seam implemented (feature dormant by design)    |
| [0042](0042-template-lifecycle.md)                         | Template lifecycle: stamp-out script, two-channel updates, @repo scope stays                        | Accepted | Implemented                                     |
| [0044](0044-security-baseline-supply-chain.md)             | Security baseline + supply chain: hardening defaults, gitleaks/audit/Trivy CI gates, ASVS L2 map    | Accepted | Implemented (gates live in CI)                  |

## Project-owned ADRs (Perimetra rebuild, ≥0045 — never touched by skeleton merges)

| ADR                                                  | Decision                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Status       | Reality                                                                                                                                                                                                                                                                     |
| ---------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --- | ------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [0045](0045-expr-numeric-domain-ieee754.md)          | Expr DSL evaluates in IEEE-754 (delta-0 requires the MVP's arithmetic); I10 money hardens in step 3                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Accepted     | Implemented                                                                                                                                                                                                                                                                 |
| [0046](0046-catalog-role-resolution.md)              | Catalog = versioned engine argument; {role, section, material} resolution is the ONE component mech                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Accepted     | Implemented                                                                                                                                                                                                                                                                 |
| [0047](0047-error-taxonomy.md)                       | Error taxonomy: author-time throws (validateRelease publish gate), config-time typed Issues + params                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Accepted     | Implemented                                                                                                                                                                                                                                                                 |
| [0048](0048-cascade-overrides-ledger.md)             | Cascade/override semantics: one write path, quote-only deviations, ledger as query, 15-sig money                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Accepted     | Implemented                                                                                                                                                                                                                                                                 |
| [0049](0049-site-graph-composition.md)               | Site graph: port sharing (owner/consumer), terrain via input gate, paired connection scopes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Accepted     | Implemented                                                                                                                                                                                                                                                                 |
| [0050](0050-renderer-layer.md)                       | Renderer layer: keyed piece geometry on PartRule, baked catalog facts, pure data renderers (I4)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Accepted     | Implemented                                                                                                                                                                                                                                                                 |
| [0051](0051-generated-configurator.md)               | Generated configurator: UiSpec on the release, browser-hosted engine, app-land R3F scene adapter                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Accepted     | Implemented                                                                                                                                                                                                                                                                 |
| [0052](0052-site-canvas.md)                          | Site canvas: generated surface at site scope, two-truths derive (footprints survive an invalid site)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Accepted     | Implemented                                                                                                                                                                                                                                                                 |
| [0053](0053-quote-lifecycle.md)                      | Quote lifecycle: immutable stores + stamps/snapshot + I3 reproduction; rebuild-core built for NodeNext                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Accepted     | Partial — I3 core (stores/quote/verify); project persistence, org retrofit, roles deferred                                                                                                                                                                                  |
| [0054](0054-project-site-persistence.md)             | Project site persistence: project.site + project_instance roster, full-document GET/PUT, /site/:projectId                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Accepted     | Implemented (persistence only; api-served catalog deferred)                                                                                                                                                                                                                 |
| [0055](0055-org-scope-activation.md)                 | Org-scope activation: auto-org per user, scoped() owner→org, NOT NULL + RESTRICT (I3), org realtime channel                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Accepted     | Implemented (scope only; roles/invite UI deferred). Supersedes ADR 0041 dormant stance                                                                                                                                                                                      |
| [0056](0056-rbac-roles.md)                           | RBAC on org membership: admin/sales/workshop role on member, RoleGuard, workshop price-blind DTO, margin-floor guard + admin override, admin publish gate                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Accepted     | Implemented (BE enforced + FE mirror; invite/role-management UI + cost-based margin deferred)                                                                                                                                                                               |
| [0057](0057-org-invite-member-sharing.md)            | Org invite + member sharing: Better-Auth-owned invite lifecycle, custom ac roles (server/client copies), invite email via email module, deterministic active-org, /team UI + switcher + accept page                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Accepted     | Implemented (personal-org wart fixed by ADR 0058; sticky-switch still deferred)                                                                                                                                                                                             |
| [0058](0058-invite-first-onboarding.md)              | Invite-first onboarding: suppress personal-org provisioning when a pending invite exists, `?next=` login round-trip; resolves the ADR 0057 wart                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Accepted     | Implemented (sticky last-active org + web self-registration deferred)                                                                                                                                                                                                       |
| [0059](0059-cost-model-real-margin.md)               | Cost model: pure engine cost layer (recipe re-evaluated against a cost table), cost co-located on the price_table row (I3, no new stamp), real `(price−cost)/price` margin + per-org floor, retires the `QUOTE_MARGIN_FLOOR_PCT` proxy                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Accepted     | Implemented (aggregate cost; per-line BOM cost + publish UI deferred)                                                                                                                                                                                                       |
| [0060](0060-api-served-catalog.md)                   | Api-served catalog: web reads releases/catalog/active-price-table from the api (RSC bundle, prop-passed), `initialInput` as gated release-row publish metadata, standalone golden-corpus seed; retires the ⌛ `@repo/fixtures` web source                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Accepted     | Implemented (per-tenant release visibility shipped by ADR 0062; new-org price-table auto-provision deferred)                                                                                                                                                                |
| [0061](0061-admin-publish-ui.md)                     | Admin publish UI: admin-gated `/admin` (JSON paste for catalog/release, structured price-table form), renders I2 `defects[]`/`issues[]`, Idempotency-Key publish mutations + published lists                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Accepted     | Implemented (publish moved to /platform by ADR 0062; release-retire + structured editor deferred)                                                                                                                                                                           |
| [0062](0062-per-tenant-release-visibility.md)        | Per-tenant release visibility: vendor-assigns via a platform actor (`user.role='admin'` + PlatformGuard); `org_release_assignment` join + tenant-scoped release list/get; publish retiered org-admin→vendor; quote-issue assignment gate; I3 ≠ visibility; `/platform` console                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Accepted     | Implemented (version-pin/opt-in-upgrade UX shipped by ADR 0064; new-org auto-assign shipped by ADR 0063)                                                                                                                                                                    |
| [0063](0063-new-org-default-provisioning.md)         | New-org default provisioning: vendor-configured `PLATFORM_DEFAULT_RELEASE_IDS` auto-assigned at runtime org-create via an AuthModule-owned hook registry (no cycle); empty-but-honest (no default price table); fail-soft + idempotent; reverses ADR 0062's deferral                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Accepted     | Implemented (vendor starter price table deferred; release version-pin shipped by ADR 0064)                                                                                                                                                                                  |
| [0064](0064-release-version-pin.md)                  | Release version pin / opt-in upgrade: `org_model_pin` (active version per model, separate from assignment); lazy-pin on assign; pinned-only configurator list; tenant admin opt-in (`/v1/releases/pin` + `/upgrades`); single-catalog pre-flight (422); `assertAssigned` stays set-membership; I3 ≠ pin                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Accepted     | Implemented (per-release catalog shipped by ADR 0065; vendor-broadcast offer shipped by ADR 0066)                                                                                                                                                                           |
| [0065](0065-per-release-catalog.md)                  | Per-release catalog (mixed versions): `deriveSite` takes a `ReadonlyMap<releaseId, Catalog>` (each instance derives against its own pinned catalog); `SiteStamps.catalogVersions` map; drops the `mixed_catalog`/`upgrade_catalog_conflict`/web single-catalog guards; `MissingCatalogError` is the new structural I5 backstop; drops the `quote.catalog_version` denorm; I3 holds across mixed catalogs                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Accepted     | Implemented (supersedes the single-catalog clause of 0060 + the `upgrade_catalog_conflict` pre-flight of 0064; vendor-broadcast offer shipped by ADR 0066)                                                                                                                  |
| [0066](0066-vendor-broadcast-upgrade-offer.md)       | Vendor-broadcast upgrade-offer fan-out: one platform action (`POST /v1/platform/releases/:id/broadcast`) makes a new version available to every org pinned to an older version of its model (server-derived targeting); reuses the single-assign path so it NEVER moves a pin; validate-once + per-org-isolated + idempotent; additive `org_model_pin(model_id)` index                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Accepted     | Implemented (closes the vendor-broadcast deferral of 0064/0065 — the last piece of the §3 release lifecycle)                                                                                                                                                                |
| [0067](0067-release-retire.md)                       | Release retire (`published`→`retired`) + platform release-detail read: `POST /v1/platform/releases/:id/retire` (vendor-only) is NON-STRANDING — a retired release stops being offered (not assignable/broadcastable/pinnable, dropped as an upgrade target) but orgs already pinned keep configuring; I3 untouched (body never mutated, row never deleted); idempotent + race-safe (200); global `GET /v1/platform/releases/:id`; account-page nav links                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Accepted     | Implemented (closes the release-retire + platform-detail-endpoint deferrals of 0061/0062/0066; structured release editor still deferred)                                                                                                                                    |
| [0068](0068-structured-release-editor.md)            | Structured release editor (model-IDE on `/platform/releases/new`, replacing the raw-JSON textarea) + `slotScopes()` single source of scope truth consumed by `validateRelease` (so the editor's autocomplete can't drift from the publish gate); `tokenize` exported for syntax highlight; publish stays the existing immutable `POST /v1/releases` (no second freeze path, I3 untouched); 4 independently-shippable phases                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Accepted     | **Phase 1 shipped** (model-IDE editor live at `/platform/releases/new`: structured params/constraints/derived + `ExprField` live-validate + hybrid JSON islands; raw-JSON form retired). Phases 2–4 next                                                                    |
| [0069](0069-identity-provisioning-decouple.md)       | Decouple org provisioning from the email+password credential: extract a credential-agnostic `provisionWorkspace` seam from the ADR 0055/0063 `session.create.before` hook so email+password signup and (future) SSO/SCIM JIT both call it — cutting the seam now (at ~1 tenant) so the eventual SSO retrofit is a build, not a tenant migration. No SSO built here; behavior-preserving                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Proposed** | Not implemented — awaiting sign-off (go-gate: touches the live tenancy seam). Enterprise-readiness Phase 0                                                                                                                                                                  |
| [0070](0070-mandatory-totp-mfa-platform-operator.md) | Mandatory TOTP MFA for the platform operator: register the dormant Better Auth `twoFactor` plugin + `twoFactor` table + `user.twoFactorEnabled` (additive migration); `PlatformGuard` gates the cross-tenant surface on role AND `twoFactorEnabled` (fresh per request), 403ing a distinct `mfa_required`; web `/two-factor` challenge + `/account/security` enroll; GDPR erasure purges the credential explicitly (anonymized user row → no FK cascade)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Accepted     | **Shipped** (Phase 0 — closes the §1 "MFA off on the most dangerous credential" gap; the password/audit half was 0.4 under ADR 0040)                                                                                                                                        |     | [0071](0071-immutable-snapshot-pii-retention.md) | Immutable-snapshot PII vs Art.17 erasure: an ISSUED quote/invoice retains buyer-identifying fields for the statutory accounting period under the legal-obligation basis (Art.17(3)(b)) — never erased, so I3 holds; erasure anonymizes only the LIVE customer entity + un-issued drafts (the ADR 0055 anonymize-not-delete pattern). Customer = erasable record of truth; issued doc = retained legal record | Accepted | Posture decided (Martin, the Phase-A prerequisite); not yet implemented — exact retained field-set + period are a non-blocking accountant check. Phase A step 1 |
| [0072](0072-brand-design-system.md)                  | Perimetra brand design system (Bombardier-derived editorial): additive role-named OKLCH brand tokens in the shared `@theme` file (warm-grey `--color-field`, flat-matte `--color-chrome`, `--color-copper` accent, `--color-deviation` amber on its OWN token, `--color-nav-active`, `--shadow-soft`, `--text-display`); new CVA kit primitives (`Panel`/`StepNav`/`Badge`/`DisplayLabel`/`IconButton`+`IconCluster`/copper `Button` variant); extends the one Tailwind v4 token system (no fork, no glass — Direction #1); applied configurator-first, full-app reskin a follow-on                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Accepted     | **Shipped** (Slice A of the v1 brand + live-3D configurator workstream; ADR 0073–0077 build the 3D techniques on this foundation)                                                                                                                                           |
| [0073](0073-procedural-profile-extrusion.md)         | Procedural profile extrusion (technique 4 — the credibility foundation): a `PieceProfile` → `THREE.Shape` (rect_tube/flat/pane exact, hollow only on a real `wall_mm`; L/U/T silhouettes) → small-bevel `ExtrudeGeometry`, `rotateY(π/2)`'d to local-X; pure-CPU (jsdom-testable), module-level LRU geometry cache owning buffer lifecycle, `<boxGeometry>` fallback for custom/no-profile; §6 amber override preserved. Plus the dev-only **headless 3D verification harness** (`/scene-lab` synthetic gate + `capture-scene.mjs` software-WebGL Playwright capture to `.verify/`) — the reusable eyes for the GPU-less box                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Accepted     | **Shipped** (recovered intact after an OOM mid-slice; first 3D slice on the ADR 0072 foundation — ADR 0074–0077 build lighting/finish/deviation/camera + Part-B layout on it)                                                                                               |
| [0074](0074-studio-lighting.md)                      | Studio lighting (technique 2 — the #1 premium carrier): procedural drei `<Lightformer>` IBL inside `<Environment background={false} frames={1}>` — invisible studio fill with NO external HDR (CSP-clean; sidesteps the blocked preset CDN + the decided self-host-`.hdr` without a binary asset), a real HDRI reserved for the §8 visible in-context backdrops; one warm key `directionalLight`; `<ContactShadows>` grounds the gate on a new `frame.groundY` (AABB floor; `frame.ts` now unit-tested); CAD grid removed; brand-field `#ededed` bg + exposure `1.4→1.0`. App-land only, no schema change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Accepted     | **Shipped** (second 3D slice; render-taste pass — metal/intensities/copper-vs-amber — owed to Martin against a GPU render, SwiftShader under-represents specular)                                                                                                           |
| [0075](0075-finish-colour-swap.md)                   | Finish/colour/material swap (technique 3 — the "Stripe Colour" analog, live not raster): a `zustand` finish slice recolours the live mesh with a synchronous prop update (no PNG-per-swatch fetch); curated CZ finishes (RAL powder colours → `material.color` auto sRGB→linear under ACES, žárový zinek via moderate metalness/roughness so it reads as light metal under the dim IBL, dřevodekor via a procedural CSP-clean `CanvasTexture`); **§6 amber stays ABOVE the finish** (a deviated piece is amber at any finish, the wood map never attaches to it); indicative RAL + "barva orientační — potvrďte dle vzorku" caveat; presentation-only (no colour parameter on the release — I1/I4 untouched), app-land R3F, no schema change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Accepted     | **Shipped** (third 3D slice; `FinishPicker` lifted into the wizard's Barva step by ADR 0077; render-taste — zinek sheen, wood-grain prominence — owed to Martin against a GPU render)                                                                                       |
| [0076](0076-deviation-surfacing.md)                  | Deviation surfacing (technique 8 — CORE_SPEC §6 invariant, not decor): a highlight toggle (`useDeviation` slice) → deviated pieces emissive amber + the rest desaturated; **always-on out-of-frustum edge markers** — per-frame `vec3.project(camera)` → a pure `placeEdgeMarker` clamp → a ref-driven DOM marker rides the viewport edge (incl. behind-camera `z>1`) so NO angle can hide a deviated piece; count badge + `DeviationPanel` reason rows (mirror of the 2D `DrawingFlag`); deviation amber unified on the brand `--color-deviation #f59e0b`; pure math unit-tested + an e2e (`?cam=away` → marker MUST appear); app-land R3F, no engine/schema change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Accepted     | **Shipped** (fourth 3D slice; ref-driven DOM overlay instead of literal drei `<Html>` for edge-clamping + zero per-frame re-render; `DeviationPanel` reused in the wizard Summary by ADR 0077)                                                                              |
| [0077](0077-camera-choreography-wizard.md)           | Camera choreography + the 5-step CZ wizard (technique 6/10 — the Bombardier UX grammar): drop `OrbitControls` → one drei `<CameraControls>` rig animating to a pure named pose per step (`smoothTime`+`setLookAt(…,true)`, interruptible; persistent canvas so it animates not cuts); the fixed brand spine Produkt · Lokalita · Konfigurace · Barva a povrch · Souhrn over the release's OWN `ui.steps` (CORE_SPEC §8 — labels/options/groups stay release data; "Výplň"→"Konfigurace" since a release authors more than infill); hybrid R3F + `drawing2d` SVG (Lokalita plan, Summary elevation w/ §6 flags, WebGL-down fallback); Souhrn = spec sheet + reproducible config-hash share (I3 tie, `?c=` restore) + lead form; pure helpers (camera-poses/wizard-flow/config-hash) unit-tested; app-land, no engine/schema change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Accepted     | **Shipped** (fifth slice — configurator v1 COMPLETE: 4+2+3+8+6; exploded/section 9 deferred to v2; old release-step `Wizard` kept for `/site`; render-taste — pacing + layout proportions — owed to Martin)                                                                 |
| [0079](0079-gap-free-numbering-series.md)            | Gap-free org-scoped document-numbering series (Phase A step 2 — the legal evidence number): a per-org/per-year `quote_number_sequence(org, year, last_number)` counter incremented by one atomic `INSERT … ON CONFLICT DO UPDATE … RETURNING` INSIDE the `issue` `@Transactional()` — gap-free (rollback undoes the increment), race-safe (serializes on the (org,year) row lock), pooling-safe (no DB sequence / advisory lock / session GUC, per CLAUDE.md); `quote.document_number` NOT NULL + a unique (org, document_number) backstop; pure `formatQuoteNumber(year, seq)` → `{year}/{seq:04d}`; the number is a row identifier, NOT part of I3 re-derivation (verify still reproduces); backfill-safe migration                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Accepted     | **Shipped** (Phase A spine; integration-covered — sequential/gap-on-failure/per-org/concurrent; invoice series is a Phase-B module; continuous-vs-per-year is a non-blocking accountant CHECK)                                                                              |
| [0078](0078-brand-typeface-trio.md)                  | Brand typeface trio (Chillax / Synonym / Amulya) — replaces the ADR 0072 "one geometric sans" placeholder: three self-hosted variable woff2 via `next/font/local` (CSP-clean app origin, Fontshare FFL), bound to role-named `@theme` font tokens — `--font-sans`=Synonym (body/UI default), `--font-display`=Chillax (display/headings/step labels), `--font-data`=Amulya (data labels/numeric), `--font-mono`=Geist Mono; each token `var(--font-<face>, "<Face>")` so web binds next/font + mobile falls back to the literal family (lazy substitution, no `@theme inline`); applied configurator-first (body sans, base-layer Chillax headings, `DisplayLabel` font-display, `Badge`/BOM/spec/data grids font-data); full Czech diacritic coverage verified headless                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Accepted     | **Shipped** (Slice 1 of the post-configurator brand work; render-taste — display weight, pairing balance, Amulya small-size weight, copper-vs-amber on a lit render — owed to Martin; specimen artifact produced)                                                           |
| [0080](0080-structured-tax-92e.md)                   | Structured §92e/DPH tax layer (Phase A — the legal-document spine): replaces the vestigial `dphRate string + reverseCharge boolean` (applied NOWHERE; the quote froze only its net) with a pure `@repo/model` tax layer — a discriminated `TaxModeKind` (`standard_vat`/`reverse_charge_92e`), `resolveTaxMode(conditions)` (§92e iff both parties CZ VAT payers AND construction/assembly), and `deriveTaxBreakdown(rateBases, mode, policy, currency)` → per-rate `{netBase,vatAmount,gross}` lines + totals + the §92e legend; §92e is a NO-VAT-line document (not a 0 % rate), decided per-TRANSACTION (so `reverseCharge` is dropped from the price table); the structured breakdown is FROZEN in the immutable snapshot + re-derived at verify (I3); CZ-tax golden corpus (21 %/§92e/mixed/rounding edges) on the real derived net                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Accepted     | **Shipped** (Phase A spine; integration-covered — issue/§92e-issue/verify-reproduces; the §92e legal conditions + legend wording + standard rate are PROVISIONAL, accountant-gated/flagged)                                                                                 |
| [0081](0081-exact-decimal-money-rebaseline.md)       | Exact-decimal money + commercial rounding policy — golden re-baseline (Phase A, interlocked with 0080): `money.ts` moves from 15-sig float canonicalisation to exact-decimal BigInt arithmetic (`roundMoney`/`addMoney`/`mulMoney`/`percentOf`) + a `RoundingPolicy {scale,mode,granularity}` threaded from a new price-table `rounding_policy` column (frozen at publish, stamped → I3), NOT a hardcoded default; the Expr/pricing domain stays float (ADR 0045 untouched), only the I10 boundary rounds to haléř; legitimately re-baselines the sub-haléř goldens `81451.504→81451.5`, `129891.504→129891.5`, `73741.504→73741.5`, `130241.504→130241.5`, accessory `31548.504→31548.5`, per-line `1023.744→1023.74` (cost `79039.86`/lamela `75174.2`/fence `24570` UNCHANGED — already ≤ haléř)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Accepted     | **Shipped** (Phase A spine; goldens + corpus + api/web suites re-baselined, reproduce through issue→verify; the new canonical numbers + the provisional policy are FLAGGED for Martin's sign-off)                                                                           |
| [0082](0082-customer-entity-per-rep-ownership.md)    | Customer entity (odběratel) + per-rep ownership (Phase A — the "C" in CPQ): a `pnpm gen module` customer flipped to org-scoped (ADR 0055) with PII columns `pii()`-wrapped (ADR 0040/0071); per-rep ownership layered ON TOP of the org scope (`scoped(scope,{restrictToOwner})` — admin sees the whole org, sales only its own; the SAME narrowing added to quotes list/get/verify); DELETE anonymizes the buyer PII in place + archives (never a hard delete) so an issued quote that froze the buyer keeps re-deriving (`quote.customer_id` ON DELETE RESTRICT, I3); `issue` attaches an ownership-validated `customerId` whose `vatPayer` auto-fills the §92e decision (frozen mode → re-derivation never re-reads the mutable customer); GDPR handler exports rep linkage (not buyer PII), erase is a no-op (org data retained, user row anonymized centrally)                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Accepted     | **Shipped** (Phase A spine; integration-covered — rep isolation / attach+§92e auto-fill / anonymize-keeps-quote-reproducible; exact retained-PII field-set/period + rep-export shape are legal-gated/flagged)                                                               |
| [0083](0083-quote-lifecycle-transitions.md)          | Quote lifecycle transitions + buyer acceptance (Phase A — close the loop): the dead status enum becomes a guarded state machine — `draft→issued→accepted` (or `→declined`), with `expired` DERIVED on read from `validUntil` (`effectiveStatus`, no cron/stored mutation); `declined` added to the text enum (no migration); the previously-UNREAD `shareToken` powers a PUBLIC `QuotesPublicController` (`/v1/quotes/shared/:token/{accept,decline}`, no SessionGuard — the unguessable token IS the credential, global throttle tightened to 10/min) gated to an _effectively-issued_ quote (409 otherwise); I3 untouched (only `status` moves; the snapshot/stamps never change) — `@Transactional()` + audited with a null actor                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Accepted     | **Shipped** (Phase A spine; lifecycle unit + integration — accept/decline/double-resolution-409/expired-409/unknown-404/derived-expiry; the buyer-facing VIEW is the quotes-web/PDF slice)                                                                                  |
| [0084](0084-quotes-web-surface.md)                   | Quotes web surface (Phase A — the "Q" gets a UI): the built-but-unreachable quotes backend made reachable on the brand kit (ADR 0072/0078). App-side query libs (mirror `createProjectsQueries`) + MSW for quotes/customers; `/quotes` per-rep list + `/quotes/:id` detail rendering the structured §92e/DPH tax breakdown (no-VAT-line + legend for §92e) + a reproducibility panel that reframes `verifyReproducibility` as a customer-facing TRUST feature; issue flow on the site canvas (`IssueQuotePanel`, sales/admin only) — customer picker + inline create + §92e toggle, buyer VAT auto-fills server-side. Branded Part-A neutral system (warm field / matte chrome / single copper accent; amber reserved for deviations); proxy prefix + nav + i18n (cs/en). No backend change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Accepted     | **Shipped** (Phase A spine; web vitest 145 — list/detail/§92e-legend/verify-trust; render-taste owed to Martin — specimen artifact published; richer issue wizard + buyer public VIEW deferred to the PDF slice)                                                            |
| [0085](0085-nabidka-renderer.md)                     | Nabídka renderer — the pure-data L layer of the split PDF (Phase A): `buildNabidka(site, result, {documentNumber, tax, customer})` → a `NabidkaDocument` (header + priced BOM line items + per-category net subtotals + the structured §92e/DPH tax document + net/VAT/gross totals + the §92e legend), off `(Site, SiteResult)` + the frozen tax breakdown + buyer + doc-number ONLY (I4, no I/O); already I10-canonical so a nabídka off a re-derived quote is byte-identical (I3). Golden-tested in `@repo/fixtures` on the real derived site (standard 21 % / §92e no-VAT-line+legend / determinism). The thin PDF/print SURFACE (M) + the customer-identity snapshot freeze are split out (flagged)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Accepted     | **Shipped (L layer)** (Phase A spine; renderer golden-tested; branded specimen artifact published as the M-layer reference; the PDF/print binary surface + the buyer-identity freeze are flagged for a fresh session)                                                       |
| [0086](0086-customer-identity-snapshot-freeze.md)    | Customer-identity snapshot freeze (Phase A — close the nabídka loop): `issue()` freezes a `customer {customerId,name,ico,dic,vatPayer,address…,country}` block into the immutable `QuoteSnapshot` (was only a mutable `customer_id` FK), realizing the ADR-0071 buyer-side posture — the issued daňový doklad retains its buyer fields under the legal-obligation basis. A CAPTURED FACT, not a derived artifact: deliberately ABSENT from `verifyReproducibility` `checks[]` (I3 holds — a since-anonymized customer still reproduces) and retained through Art.17 (live row anonymized→404, frozen buyer survives). Document-identity fields only (email/phone/note excluded — data minimisation); workshop-blind (not whitelisted); snapshot stays `z.unknown()` passthrough — no migration/validator change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Accepted     | **Shipped** (Phase A; quotes integration — freeze / erase-retains+I3-reproduces / §92e-from-customer; the exact retained field-set + statutory period are accountant-gated/flagged)                                                                                         |
| [0087](0087-nabidka-print-surface.md)                | Thin PDF/print nabídka surface — the M layer (Phase A — close the nabídka loop): a web print route `/quotes/:id/nabidka` (RSC) + presentational `NabidkaDocumentView` lays out the L-layer `NabidkaDocument` (ADR 0085) as a branded A4 sheet (header + buyer block from the ADR-0086 freeze + priced line items + per-category net subtotals + the §92e/DPH tax document + legend), on the Part-A brand system + the app-origin ADR-0078 fonts (CSP `font-src 'self'` holds); `window.print()`→PDF (no PDF dep). The RSC builds off the FROZEN snapshot (no re-derive → byte-consistent with issue, I3); `buildNabidka` `result` narrowed to `Pick<SiteResult,"bom"\|"money">` so the snapshot feeds it directly; price-blind workshop 404s (no tax/money). No backend change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Accepted     | **Shipped** (Phase A; web vitest — standard/§92e-legend/no-customer; supplier/dodavatel block is a FLAGGED placeholder — org legal profile is the next follow-on; buyer-facing public view deferred; render-taste owed to Martin — specimen published)                      |
| [0088](0088-org-legal-profile.md)                    | Org legal-profile + supplier-identity snapshot freeze (Phase A — closes the nabídka loop): an org-scoped SINGLETON `legal_profile` (name/IČO/DIČ/vatPayer/sídlo/bank/registration-note), `GET/PUT /v1/org/legal-profile` (admin-gated; `{profile:null}` empty-state, not 404), replacing the ADR-0087 supplier PLACEHOLDER. `issue()` FREEZES a `supplier` block into the immutable snapshot symmetric to the ADR-0086 buyer — a captured fact, ABSENT from `verifyReproducibility` `checks[]` (a since-edited org profile never retro-alters an issued daňový doklad, I3); its `vatPayer` drives §92e (retires the hard-coded `supplierVatPayer:true`). No profile ⇒ `422 legal_profile_required` (no honest legal doc without a supplier). NOT `pii()` (business identity); threaded through renderer L (`NabidkaSupplier`) + print M + an admin form at `/team/legal-profile`; additive migration                                                                                                                                                                                                                                                                                                                                                                                                                              | Accepted     | **Shipped** (Phase A; integration — freeze / edit-then-verify-isolation / no-profile-422; §29-ZDPH field-set + OSVČ-PII posture legal-gated/flagged; render-taste owed to Martin)                                                                                           |
| [0089](0089-buyer-public-nabidka-view.md)            | Buyer-facing public nabídka view (Phase A — closes the loop end-to-end): a no-session, shareToken-credentialed `GET /v1/quotes/shared/:token` on the existing `QuotesPublicController` (inherits the 10/min throttle) + a web route `/nabidka/[token]` (OUTSIDE the `/quotes` protected prefix → no proxy change) reusing `NabidkaDocumentView` verbatim (additive optional `actions`/`backHref` slots) + accept/decline. The load-bearing decision is the trust boundary: the backend BUILDS the `NabidkaDocument` server-side off the frozen snapshot (the same pure `buildNabidka`, no re-derive → I3-consistent) and returns ONLY `{document,status,validUntil}`, so the snapshot's cost/margin (ADR 0059), re-derivation seeds, and I3 stamps NEVER cross to the unauthenticated buyer (allowlist-by-construction + DTO strip; asserted by a raw-payload non-leak test). Read does NOT 409 a resolved/expired quote (the buyer must always see their offer); unknown token 404s. No schema change                                                                                                                                                                                                                                                                                                                            | Accepted     | **Shipped** (Phase A; integration — build/no-leak/post-resolve-200/expired-200/unknown-404 + web vitest issued↔resolved↔expired; PII posture unchanged — buyer sees only their own frozen identity; render-taste owed to Martin)                                            |
| [0090](0090-registry-lookup-ares-vies.md)            | ARES/VIES registry lookup (Phase A tail): a read-only, fail-soft api proxy over two PUBLIC registers — CZ **ARES** (IČO → name/sídlo/DIČ) and EU **VIES** (DIČ → VAT-payer validity, the §92e evidence) — in a new `lookups` module (controller + thin service, NO schema/repository/outbox; shape precedent `PlatformModule`, fail-soft precedent `RealtimeService`). `POST /v1/lookups/ares` `{ico}` + `/vies` `{dic}` (admin/sales, `@Throttle` 30/min; the key rides in the BODY not the URL, so it never lands in the request log). **Fail-soft is the contract**: upstream 5xx/timeout (`AbortSignal.timeout`)/parse → `unavailable`, ARES 404 → `not_found`, never an exception — a register outage degrades the convenience, never blocks manual entry. A malformed key is the one client error (`400`, validated by the CZ `ico`/`dic` primitives before any outbound call). VIES **`unavailable` ≠ `invalid`** (an `MS_UNAVAILABLE`/timeout must not read as a bad number → would wrongly deny §92e). No PII to Redis/jobs/logs (IČO/DIČ never logged). Web: `aresPrefill` into the customer + supplier forms, a `<ViesBadge>`; `vatPayer` stays the rep's explicit toggle (NOT auto-set). New env `ARES_BASE_URL`/`VIES_BASE_URL`; `@repo/validators/lookups` (+ mock route group). No schema change; I1–I11 untouched | Accepted     | **Shipped** (Phase A tail; api unit `vi.spyOn(fetch)` — found/not_found/unavailable/timeout/dissolved/400/no-PII-log + VIES valid/invalid/MS_UNAVAILABLE; web — aresPrefill/viesTone/ViesBadge; §92e legal constants accountant-gated/flagged; vatPayer-from-VIES deferred) |

| [0091](0091-configurator-exploded-view.md) | Configurator exploded view (v2 slice 1 of 3 — the explode half of the "exploded/section view 9" deferred by ADR 0077): a pure app-land "bloom" presentation, NO `model`/`engine`/`renderers`/`fixtures` change (I1–I11 untouched, goldens reproduce). `pieceExplodeOffsets(scene)` (pure, unit-tested) maps each piece id to a full-explode displacement off the instance's piece-cloud centroid (perimeter pieces travel far, central barely move → the arrangement stays legible); the spread + iso camera distances are NAMED constants tuned in the render-taste pass, not frozen values. A `useExplode` slice holds a discrete `target` (toggle/slider) + a live `factor` an in-Canvas `ExplodeAnimator` damps toward (dep-free `MathUtils.damp`, settles); the walker positions each piece by `explodedPosition(at, offset, factor)`; the camera switches to a pulled-back isometric `exploded` pose keyed off `target` (stable mid-transition) and stays user-interruptible (ADR 0077). **§6 holds**: `deviatedPieceCenters` gained an optional `offsets` map (bloomed centres) and the projector lerps each marker between assembled↔bloomed by the live factor — no deviated piece can hide through the explode. Release-authored explosion recipes deferred (would need schema + publish gate) | Accepted | **Shipped** (configurator v2 slice 1; web vitest — pure bloom math / centroid / §6 marker tracking / exploded camera pose; visual calibration owed to Martin's render-taste eye) |

| [0092](0092-configurator-section-view.md) | Configurator section / cutaway view (v2 slice 2 of 3 — the section half of the "exploded/section view 9" deferred by ADR 0077): a pure app-land three.js clipping plane that slices the pieces so the extruded profiles reveal their hollow cross-sections. NO `model`/`engine`/`renderers`/`fixtures` change (I1–I11 untouched, goldens reproduce). Per-material clip on the pieces only (`gl.localClippingEnabled` + each material's `clippingPlanes`/`clipShadows`), so the studio shadow/IBL stay whole. `sectionPlane(frame, axis, position)` (pure, unit-tested) maps 0..1 across the world AABB (`frameScene` now returns the raw `min`/`max`) → `{normal, constant}` keeping the lower-coordinate half. ONE stable `THREE.Plane` mutated each frame by an in-Canvas `SectionPlaneRig` off `useSection.getState()` (never a subscription); the `clippingPlanes` array is a stable ref switching only on the on/off toggle, so an axis/position SCRUB updates the clip live with zero walker re-render. **`SceneRenderer` is now `memo`'d** (the load-bearing fix): the canvas re-renders per scrub for the UI, but the walker's props stay stable so the piece tree never reconciles — finish/deviation still re-render via internal subscriptions. 2D section-cut drawing + release-authored presets deferred | Accepted | **Shipped** (configurator v2 slice 2; web vitest — sectionPlane per-axis/clamp + frame min/max; adversarially reviewed; visual calibration owed to Martin's render-taste eye) |

| [0093](0093-configurator-preset-scenes.md) | Configurator in-context preset scenes (v2 slice 3 of 3 — the §8 "visible in-context backdrops" deferred by ADR 0074): a chosen scene swaps the viewport backdrop so the gate reads as installed (driveway / fence line / garden) instead of floating in a studio. Pure app-land, NO `model`/`engine`/`renderers`/`fixtures` change (I1–I11 untouched, goldens reproduce). **Procedural geometry, keeping the studio IBL** (per the v2 fork — a self-hosted HDRI would add a binary + a CSP origin; this stays CSP-clean, the Lightformer precedent). A `useScene` slice + curated `SCENES` data (mirrors `finish.ts`; labels are domain data, not i18n); a `SceneBackdrop` sized off the frame renders a ground plane a hair below `groundY` (so `<ContactShadows>` reads as the gate's shadow on the floor, no z-fight) + light context (masonry pillars / receding fence posts / a low hedge); `studio` renders nothing (the v1 field); the per-scene sky drives the Canvas background. The section clip is per-material on the PIECES only so the backdrop is never clipped, and the `memo`'d walker's props don't change on a scene switch so swapping scenes never reconciles the piece tree. Vendor-authored default scene per model deferred (schema + publish gate). **Completes configurator v2** (explode + section + scenes) | Accepted | **Shipped** (configurator v2 slice 3; web vitest — sceneById lookup/fallback/studio-default; adversarially reviewed; all visuals owed to Martin's render-taste eye) |

| [0094](0094-configurator-v2-ux-polish.md) | Configurator v2 UX polish (render-taste pass; refines ADR 0091/0092): on the live review the explode "didn't make sense" + the sliders didn't align with their toggles — features kept (still the playground), just polished. Pure app-land, NO `model`/`engine`/`renderers`/`fixtures` change (I1–I11 untouched, goldens reproduce). **Explode now blooms BY PART, rigidly**: `pieceExplodeOffsets` groups pieces by their `partPath` (the stable middle segment of the I9 id) and gives every piece of a part ONE shared offset (assembly-centroid → part-centroid × spread), so the frame/infill/lock each travel as a unit instead of scattering per-piece; a single-part assembly no longer explodes; offsets still sum to zero; §6 marker lerp unaffected (offset map stays per-piece keyed). **Controls align**: each scrub control sits in its own fixed `h-8` row (matching the `size-8` toggle) at `items-center`, present-but-empty when off, so the explode slider lines up with the explode toggle and the section row with the section toggle. Spread/depth-stagger/section-default-axis stay tunable; the X/Y/Z scrubber is acknowledged playground (kept by Martin's call) | Accepted | **Shipped** (configurator v2 polish; web vitest — rigid-part grouping / single-part no-op / balanced / distinct part paths; adversarially reviewed) |

| [0095](0095-sliding-gate-geometry-correctness.md) | Sliding-gate 3D geometry correctness + a geometry-position golden (core-hardening slice 1, after the release-reality-check redirect away from polishing the strong side). The configurator looked broken ("models floating in air, not Excel quality") because the v2 slices polished a gate whose AUTHORED geometry was wrong and NOTHING pinned piece positions. Got eyes on the real render (`verify-3d-headless` — Playwright + SwiftShader capture, read the PNGs) and cross-checked the working gates-MVP 3D + the Excel: (1) the suspension diagonal `rotation 180 − angle` ASCENDED to y≈2530 attached to nothing (→ `180 + angle`, descends to the rail); (2) the 6.5 m Nosník V floated as a full-width OVERHEAD beam at `clear_height+60` (it's the LONGEST member — the ground carrier; seated at ground); (3) studio had `ground:null` so the gate hung over a void (→ a real floor; every scene now grounds). **The systemic fix is a geometry-POSITION golden** (`sliding-gate.geometry.test.ts`) asserting the assembled envelope (no leaf piece above clear height — catches both historical floats; diagonal descends; catch post grounded) — the guard whose absence let wrong geometry ship green. `/scene-lab` now drives the REAL fixture (was synthetic). **BOM/price unchanged — goldens reproduce (`81451.5`/`81849.192`), I1–I11 untouched** (placement is presentation off the same derivation, I4). FLAGGED for fidelity slices: diagonal cut angles (Excel 55/17,5 vs authored angle/90), catch-post height (1900 vs gates-MVP clear_height), Nosník V exact role, box-fallback profiles | Accepted | **Shipped** (core-hardening slice 1; fixtures geometry golden + delta-0 reproduce; web scenes test; visually verified via headless capture; cut-angle/material fidelity flagged for next slices) |

| [0096](0096-configurator-materials-environment.md) | Configurator materials + environment relight (core-hardening slice 2): the default antracit (RAL 7016) powder finish read as a flat BLACK silhouette ("material none / environment bad"). The cause is LIGHTING, not the material — the perimetra antracit hex is identical to the WORKING gates-MVP's, but the studio IBL was tuned so dim a dark albedo had no diffuse floor. Pure app-land relight matching gates-MVP's proven light levels while keeping the procedural CSP-clean IBL (no binary HDRI — ADR 0074, and SwiftShader can't verify a real HDRI's specular gain headlessly anyway): the dark-lift comes from FILL (ambient 0.3→0.55 + a NEW sky/ground `hemisphereLight` + a NEW cool fill directional), not global exposure (1.0→1.18, so bílá doesn't blow), Lightformer IBL intensities +~1.3×, studio ground →`#b8b3ab` mid-concrete + contact-shadow 0.5→0.6 so every finish separates from the floor. NO `model`/`engine`/`renderers`/`fixtures` change (I1–I11 untouched, goldens reproduce). Verified eyes-on (the standing rule) across antracit/bílá/zinek/dřevodekor. **Box-fallback sections DEFERRED-as-gated** (a catalog `section` feeds the FROZEN cutList + 2D drawings → rebaselines those goldens = Martin sign-off + new catalog version; the V-rail is FIL-blocked); **catalog-driven finishes DEFERRED** (app-land but a real design call — global finish vs mixed-material gate, marginal value) | Accepted | **Shipped** (core-hardening slice 2; full gate green + goldens reproduce; visually verified via headless before/after capture; box-sections + catalog-finishes flagged-gated; render-taste of the rig owed to Martin's eye live) |
| [0097](0097-sliding-gate-vyplet-fill-types.md) | Complete the seven cantilever Výplet fill types + a second Excel anchor (core-hardening slice 3). Only 2 of 7 infill types were authored, and the live price table lacked even `lamela_113` (selecting it → I5 missing-price). Transcribed all seven from two agreeing ground truths — the 2026 Excel `Výplet` sheet + the gates-MVP `fillType` seed: `lamela_113_3d`, `lamela_120_3d`, `planka_120_3d`, `lamela_113_2d`, `planka_120_2d`, `planka_100_3d`, `planka_100_2d` (JAKL deferred — manual count + distinct tube spacing). Each option carries the full Výplet record; the placement attrs (`end_offset_*`, `max_spacing_mm`, `max_overlap_mm`, `disable_max_spacing`) are authored for completeness but INERT until the next slice's real spacing engine (today geometry still stacks at `min_spacing_mm`). ONE catalog component per physical profile (Excel shows identical `cena/m` for a profile's 2D/3D pair) → two new sections/components `lamela_120`+`planka_120` (alu+steel, flow into catalog@2); `validateRelease` unchanged (`fill.section_code` resolves at derive time). Locked a SECOND real Excel anchor `lamela_113_3d_5m` = `83522.44` (raw U34 `83522.442`, 5m-výroba sheet, reproduces byte-for-byte) + 5 per-type regression cases (engine-derived totals). Backfilled the live price (real Excel H-column) + demo-cost tables so all seven price live. **NO `model`/`engine`/`renderers`/web change — I1–I11 untouched; every pre-existing golden reproduces UNCHANGED, no re-baseline.** FINDING: the 3rd workbook (`81849.192`, loosely cited in 0095/0096 but never code-locked) is NOT a valid anchor — its rail formula is `=Q4*1.334`, a VZOR-sample typo vs the canonical `1.333`; the engine is correct. FLAGGED: demo fill costs (real costs = FIL slice); new types' RENDER eyes-on folded into the spacing slice (no geometry template change here) | Accepted | **Shipped** (core-hardening slice 3; full gate green + api integration 134/134; case-3 derived byte-exact to Excel U34; all 9 goldens reproduce; fill costs + spacing-render flagged) |
| [0098](0098-vyplet-spacing-engine.md) | Výplet spacing engine — the Excel `Kalkulace` slat-placement math (five derived keys `fillGaps`/`fillRawPitch`/`fillPitch`/`fillRemainder`/`fillOffset1` feeding the fill `at.y`; raw pitch over clear-span-minus-end-offsets, capped at `max_spacing_mm` unless the 3D fill disables it, remainder re-centred; `roundUp` Expr fn; attr-only pre-derivation `sliding.fill.overlap_within_max` warn guard). Documents the as-built engine checkpointed in `83ffc65` so the three in-code `ADR 0098` citations resolve. Count formula UNCHANGED → BOM/price frozen, all goldens reproduce; only slat POSITIONS change (I4). **Proposed, NOT golden-locked** — the 2026-07-02 model-truth wave (CAR-18/25) re-derives the whole sliding-gate assembly from FIL's Excel and the spacing math is in that scope; locking now would force a rebaseline. The per-fill regression lock + eyes-on captures are deferred CAR-18 deliverables; this ADR flips to Accepted (or is superseded) after Martin's pick | Proposed | **In tree, unverified** (as-built WIP folded into the model-truth track; spacing math has zero regression coverage pending the Excel re-derivation; scene-lab narrowing bug fixed separately at CAR-11 `3b91317`) |
| [0099](0099-authz-default-deny-app-guard.md) | Authz default-deny (drained from skeleton ADR 0045, renumbered — perimetra's 0045 is the Expr numeric domain): SessionGuard becomes a global `APP_GUARD` so EVERY Nest route requires a session unless explicitly opted out via `@Public()` (health probes + the anonymous buyer surface `/v1/quotes/shared/:shareToken`, where the unguessable token is the credential). Class/handler guards (RolesGuard/PlatformGuard) keep composing on top — global → class → handler ordering preserves throttle-then-auth and session-before-role resolution; redundant class-level `SessionGuard`s dropped. Generator template scaffolds `@UseGuards(RolesGuard)` so a generated org-scoped module keeps role enforcement | Accepted | Implemented (channel-A drain 2026-07-03; integration suite proves the anonymous buyer path + probes) |
| [0100](0100-fixture-version-bump-cutover.md) | Fixture-mutation → version-bump cutover (CAR-31): I3 immutability becomes STRUCTURAL before FIL's first real stamped quote. (1) BEFORE-UPDATE triggers on the three I3 stores raise on any value change to the frozen columns (release identity/body/catalog*version; catalog version/body; price_table org/version/currency/dph_rate/rounding_policy/table/cost) — lifecycle metadata (status, initial_input, effective window, margin floor) stays mutable; (2) the seed's silent skip-on-conflict becomes compare-then-skip — a DB row differing from the HEAD fixture is a hard error naming the remediation (version bump, or dev `seed:reset`+`seed`); (3) the dev loop stays cheap (in-place edit + reset while nothing references the rows; FK RESTRICT is the boundary), and a REAL model change ships as `model@N+1` through the ADR 0064–0067 lifecycle (CAR-18 → `sliding-gate@2`) | Accepted | Implemented (trigger itests in releases/price-tables; goldens untouched) |
| [0101](0101-workshop-production-view.md) | Workshop production view (CAR-24): a NEW `GET /v1/quotes/:id/production` + `/quotes/:id/production` — cut list/BOM-quantities/2D-drawings off the FROZEN snapshot (I3, never re-derived), ROLE-INDEPENDENT (admin/sales/workshop see the identical price-blind shape — production is a surface, not a permission level). `scopeOpts` widened (`restrictToOwner: role === "sales"`) so workshop's list/detail/production — previously scoped to zero rows since workshop never owns a quote — now see the whole org. A hand-mirrored, fully STRUCTURED `quoteProductionSchema` (`@repo/validators`, zero-dep leaf — cannot import renderers/engine/model) replaces opaque passthrough for this surface; closes a narrow leak vector where a commercial `pricePerUnit`/`totalPrice` artifact override could otherwise ride a `DrawingFlag` through the whitelist (`productionSafeDrawing`, applied to `blindSnapshot` too). Gated to effectively issued/accepted (404 on draft/declined/expired — no existence oracle). Non-leak itest introduces a recursive key-scan idiom. Web reuses the configurator's `WorkshopDrawingSvg`/`SitePlanSvg` + adds the app's first cut-list UI; nav needed no registry change (the "quotes" entry was already workshop-visible) | Accepted | Implemented (unit + integration + web tests green; goldens/I3 untouched) |
| [0102](0102-drawing-from-geometry-emitter.md) | 2D technical drawing = a fourth pure-data emitter off the ONE geometry SoT (CORE_SPEC §5), a sibling of cut-list/scene/BOM — kills 2D↔3D drift, multi-view free, Excel-number-true. Pipeline of pure stages (SolidModeler → ViewProjector⊕HLR → Sectioner → Annotator → DimensionSolver → Orchestrator) off `DerivationResult`, I1/I4/I5-clean, byte-deterministic (I9). Rules are IMMUTABLE release data (`DrawingSpec`, sibling of `ui?`): a rule binds a dim/chain/label to a named I9 piece + prints a derived-scope key, so the drawing prints the number the engine derived (I3-frozen, re-derivable). `drawScopes` publish-validates it as a zero-drift sibling of `slotScopes` (shared `pieceGlobToRegex` with the runtime Annotator). ProfileLibrary made the single cross-section authority — the 3D walker converges onto `profileEnvelope` NOW (byte-identical; ADR 0095 golden + scene tests hold). Honest section degrade: no `wall_mm`/`d_mm` → outer outline flagged `nominalDepth`, never an invented wall (I5). Proven on `branka@1` (elevation dims Excel-true, one hatched section, structural self-golden, 2D+3D eyes-on). Gates M4 breadth (CAR-32/33/34) | Proposed (architecture decided; Accepted gated on Martin's eyes-on) | Implemented (spike; full gate green — branka 1×SP geometry-complete only) |
| [0103](0103-fence-family-fil-ploty.md) | The fence family `fence-run@1` is the FIL `Ploty` calculator as data (CAR-32) — replaces the `anchored:false` aluminium-only harness fixture that emitted non-FIL numbers (the top FIL-usability blocker). A fence-run instance = ONE uniform run (the Excel per-field "Pole"); the SITE composes several runs sharing boundary posts (the multi-field fence). Input `run_length_mm`, bay subdivision derived (`roundUp(len/2500)`). The Výplet spacing is the SHARED ADR-0098 chain fed the RAW clear height (the h-profil carrier length `fillZoneHeight` is the chain OUTPUT, Excel F26), with the `Ploty` fill numbers (NOT the Branky/Samonosna sheets'). Fence labour is FLAT per field (Výroba 500, Montáž 650) as dedicated components, NOT hours×rate. Posts (Sloup 100 @1080/m) split start/end/line for the shared-post rule (byte-true summed rounding coincides at whole-metre heights → golden H=2000). Steel real (catalog@2 extended). Authors a `DrawingSpec` → free 2D (ADR 0102). Byte-true golden: LAMELA 113 3D bay 2000×2000 → 63756.8 (Excel Pole-1 K32=21/F26=1987/J33=94). Re-baselines the site golden 129891.5→134723.5. Documented follow-ups: shared-post shares metal not caps; `pref.mezera`/no-posts deferred | Accepted | Implemented (7-fill spacing + priced + drawing goldens; geometry eyes-on per fill; full gate green) |
| [0104](0104-deployment-tier-vercel-target-env.md) | Deployment tier derived from `VERCEL_TARGET_ENV` (drained from skeleton ADR 0046, renumbered — perimetra's 0046 is catalog/role resolution): the dev-mock and data-source gates key off a derived `TIER !== "prod"` instead of `NODE_ENV`, because every Vercel deploy (preview included) builds with `NODE_ENV=production` — a mock-leak-to-prod class of bug. `resolveTier(vercelTargetEnv, appTierOverride)` in `@repo/config/env/web`; `VERCEL_TARGET_ENV` always wins (never hand-overridable on Vercel), `APP_TIER` covers the non-Vercel container path, default `preview` (fail-safe: an unknown environment is never live). `assertTierInvariants` runs at `next.config.js` load so a tier/env contradiction fails `next build` before a chunk is emitted (build-time half), with the `TIER` gates as the runtime half. Perimetra deviates on the preview-tier invariants (see the ADR): upstream forbids `API_URL` on preview because the skeleton is a mock-first demo, which would make `next build` unpassable here — the ambiguous-data-source hazard behind that rule is kept, the mock-first assumption dropped | Accepted | Implemented (channel-A drain 2026-07-09) |
| [0105](0105-calendar-date-display-input-convention.md) | Calendar-date convention (drained from skeleton ADR 0047, renumbered — perimetra's 0047 is the engine error taxonomy): `formatCalendarDate` pins `Intl.DateTimeFormat` to `timeZone: "UTC"` so a date-only value renders as the same calendar day in every viewer's zone, and `toLocalIsoDate` builds `YYYY-MM-DD` from local clock parts (never `toISOString`) so a date default is the user's today. Rule: date-only values use the calendar helpers; true instants keep `formatDate`/`toIsoDate`. Extends ADR 0023 — no date-math library | Accepted | Implemented (channel-A drain 2026-07-09) |
| [0106](0106-pre-push-test-gate.md) | Pre-push runs the test gate (drained from skeleton ADR 0048, renumbered — perimetra's 0048 is the cascade/overrides ledger): lefthook pre-push runs `pnpm turbo run test`, matching the correctness gates CI runs, because a direct push to a deploy branch triggers no CI and a red test would ship unnoticed. `build` stays CI-only (the slow gate; a broken build fails the deploy anyway, and gating on it invites `--no-verify`). The hook is best-effort by design; branch protection is the unbypassable server-side complement | Accepted | Implemented (channel-A drain 2026-07-09) |
| [0107](0107-tenancy-app-level-scoping-not-rls.md) | Tenant isolation stays app-level (drained from skeleton ADR 0049, renumbered — perimetra's 0049 is site-graph composition): RLS / FORCE-RLS is rejected because its session-GUC requirement is incompatible with the transaction-pooling doctrine (ADR 0038). The guarantee is org-scoped repositories (ADR 0041/0055) plus every endpoint returning through a zod response schema (strip semantics), so an un-scoped `select()` still cannot ship a foreign row's fields (ADR 0039). Records perimetra's as-built posture too — a repo adopting RLS needs its own superseding ADR | Accepted | Implemented (as-built; no code change) |
| [0108](0108-workshop-traveler-frozen-technical-drawing.md) | The workshop traveler (CAR-124): the ADR-0102 technical drawing is FROZEN at issue as a top-level `technicalDrawings` snapshot field (a sibling of `drawings`, never nested — `drawings` is deep-equal-compared, so nesting would retroactively break I3 on historical quotes), and its `verifyReproducibility` check is added only when the frozen snapshot carries it (expand/contract N−1 tolerance). Frozen `specRows` are valued from a price-free scope: `buildScope` seeds the price layer, so a parameter defaulting to `price.*`would print a CZK figure on the price-blind sheet — with no price layer in scope it cannot resolve and renders "—" (absence, not masking; a regression test pins it).`toProduction`projects both plus derived`dimensionRows`through structured zod schemas (never`z.unknown()`). Dimension rules gain an optional Czech `label`; `TechnicalDrawingSvg`is the first renderer for`TechnicalDrawing`(annotation-inclusive viewBox, non-scaling strokes, dashed nominal-depth cuts);`/quotes/:id/production/traveler`prints it via`window.print()`off the existing price-blind endpoint.`dimsolve`lane offsets became span-proportional (absolute mm stacked captions on an 8 m fence run) | Accepted | Implemented (full gate green; eyes-on captures of branka + fence-run; golden moved by one number — a label anchor) |
| [0109](0109-order-domain-and-numbering.md) | The order domain (CAR-156, first R2 slice; realizes ADR-O1):`order`is a thin REFERENCE entity over an accepted quote's frozen snapshot — never a second frozen truth, no snapshot column, RESTRICT FKs for I3 durability.`order*quote_active_uq`(partial unique on`quote_id WHERE status <> 'cancelled'`) makes "one live order per quote" structural (concurrent create → one winner + 409 `order_exists`; a cancelled order frees the quote). State machine `confirmed → in_production → completed`, `cancelled`from either non-terminal state, via a pure`order-lifecycle.ts`(single-purpose`can*`predicates, no transition table) + server-side actor gates (workshop starts/completes, admin cancels with a required reason). Creation guards the quote is effectively`accepted`through`QuotesService`(cross-module read, never a schema join, ADR 0032). The workshop production/traveler re-homes to`GET /v1/orders/:id/production`, resolving order → quote → snapshot and reusing `production.ts`verbatim (byte-identical to the quote-keyed read;`/quotes/:id/production*`kept N−1). IDs-only outbox`order.*`events in the state-change tx. The ADR 0079 allocator generalizes to a shared`document*number_sequence(org, series, year)`+`NumberingService`(order series`Z{year}/NNNN`, operational not statutory); the quote counter migrates in at O2-a/CAR-157. **CAR-158** adds quote revision/supersession + order re-point: `POST /quotes/:id/revise`re-derives a new snapshot stamped`revisionOfId` and supersedes the old in one tx via a conditional update (`WHERE superseded_by_id IS NULL`) → race-safe linear chains (`superseded_by_id`UNIQUE); the superseded buyer token still renders the old doc (a`superseded`banner flag) but 409s`quote_superseded`on resolve;`POST /orders/:id/repoint`is`confirmed`-only to an accepted forward member of the chain (a `supersededById`walk), else 409. The revised + superseded quotes both still reproduce (I3 — supersession is a pointer, not a mutation) | Accepted | Implemented (full gate green; itests owed a Docker-up run; web production re-key + revise/supersede UI are the paired follow-ons) |
| [0110](0110-deviation-exception-ledger.md) | The deviation/exception ledger (CAR-159; realizes ADR-O4):`deviation_ledger`is a REBUILDABLE write-through PROJECTION (not a legal record — org FK CASCADEs, rows disposable). Rows written in the source act's tx:`quote_override`(a pure`projectQuoteOverrides(quoteId, snapshot)`at issue — no-op for the override-free golden quote, so reproduce tests are unaffected),`margin_override`(alongside its audit row),`order_exception`(order cancel-in-production auto-writes one;`POST /orders/:id/exceptions`records substituted material/site deviation).`POST /quotes/rebuild-deviations`(admin) re-projects the snapshot-derivable rows atomically, leaving the authoritative margin/order rows untouched (drift-repair by construction); orchestrated by`QuotesService`(owns the snapshots) so the module dep stays ONE-WAY (quotes/orders → ledger, no cycle).`GET /ledger?target=&from=&quoteId=`returns rows + the engine's pure`recurrenceReport`(ADR 0048, mapped back to`Override`s) — admin-only (carries margin values). The ENGINE enforces deviation bounds; the ledger only records + reports (no approval chain) | Accepted | Implemented (full gate green; itest owed a Docker-up run) |
| [0111](0111-design-system-scale-and-component-kit.md) | Design-system scale extension + component-kit expansion (extends ADR 0072/0078, not a re-brand): the ADR 0072 foundation was minimal (one radius, two shadows, only `--text-display`) and the kit was half-migrated (Button default/Toast/field inputs still stamped-shadcn — hard borders, `shadow-lg`, `rounded-md`, a leftover **blue** `--color-primary`/`--color-ring`; Toast's four statuses pixel-identical). Extends the shared OKLCH `@theme` (ADR 0004, dark parity) into a full system: semantic **radius** scale (`rounded-inset/control/card/card-lg`, never Tailwind's numeric scale), **elevation** (`shadow-soft-sm`/`shadow-float`), **type** (`text-title`/`text-metric`), **motion** (`--ease-brand`), **status** colours (success/warning/info + `-subtle`, with `warning`a SEPARATE token from the CORE_SPEC §6`deviation` amber so the domain plane never aliases), a scoped steel-blue **spotlight** accent (`#96ADC2`, hero/metric cards only), and RETIRES the shadcn blue (`--color-primary`→ink, `--color-ring`→copper — de-blues every default button + focus ring app-wide). Brings the split-brand primitives onto one grammar (public APIs preserved for ~35 button + 8 form consumers) and expands the kit with ~12 compound components on the Vercel composition patterns (compound + `React.use()`context, explicit variants, children-over-render-props, no`forwardRef`, `data-slot`, `radix-ui`, no new deps): SegmentedNav, StatCard, Field (+Input/Textarea, compound successor to the render-prop FieldShell), Select, Switch/Checkbox, Tooltip, Popover, Pager, Tabs, Dialog/Sheet, Skeleton/Spinner/Separator, EmptyState. Eyes-on via a dev-only `/brand-lab`gallery (404 in prod, the`/scene-lab`·`/drawing-lab`pattern) +`capture-brand.mjs`headless screenshots (light + dark). No app-surface reskin — that adoption sweep stays the ADR 0072 follow-on | Accepted | Implemented (kit + tokens green; eyes-on captures light+dark; surface adoption deferred) |
| [0112](0112-invoice-frozen-document-class-and-tax-cz-seam.md) | Invoice as the second frozen document class + the`@cardo/tax-cz`seam (finalizes ADR-O2 **to the seam**; realizes CAR-157/160/191/192). The`invoice`row freezes`facts`(builder inputs,`InvoiceFacts`) + `snapshot` (`ExportableDocument`, builder output) — immutable, RESTRICT FKs, `invoice_order_active_uq`partial-unique, its own gap-free`"invoice"` series (`FV{year}/NNNN`, accountant-gated). Per Martin's no-duplication ruling (2026-07-14) + order-cycle ruling 3, the CZ tax logic is NOT Perimetra's to write: `@cardo/tax-cz` owns the VAT math (`computeLineVat`/`computeDocumentTotals`, integer haléře), the §29 primitives, and every serializer behind `DocumentExporter.render(doc, issuer)`; Perimetra owns only the **consumer mapper** (frozen quote snapshot + `InvoiceFacts`→`ExportableDocument`/`ExportableLine`/`ExportableIssuer`) + the I10-decimal-koruna↔haléře conversion (the kernel's own `CONTEXT.md`: "row→POJO mapping stays in the consumer; adding a vertical = a new mapper, not a fork"). Rate is data (CAR-192: quote-stamped `dphRate`→`InvoiceFacts.ratePct`→`regimeForRate`, never a code constant). Payment posture (ADR-O3): bank transfer + admin `mark-paid`(row state, not document content) + IBAN-required 422 gate + VS via`variableSymbolFromNumber`; SPAYD/QR is kernel-owned. `verifyInvoiceReproducibility`re-runs the mapper + build and deep-equals the snapshot (I3 for the money document; the quote still reproduces`129891.504`). **Design-only:** no invoice code ships — O2-a (numbering consolidation) is Docker-blocked (untested legal-numbering migration), O2-b (module + mapper) is blocked on `@cardo/tax-cz`publishing (unpublished;`buildInvoice`/`buildSpayd`/the frozen `ExportableDocument` shape are in flight) | Accepted (design) | Design finalized against the real kernel interface; build sliced O2-a (blocker: Docker) / O2-b (blocker: kernel publish) / O2-c |
| [0113](0113-production-deploy-topology.md) | Production deploy topology (ADR-D1, the R0 deploy spine; resolves the CAR-40 host/region fork with the HQ ruling). **Railway EU** for the API's three deployables (`api-http`/`api-worker`/ a release-phase one-shot`api-migrate`, ADR 0031, from one image) + managed EU Postgres/Redis/object-storage; **Vercel `fra1`** for the web (intra-EU same-origin `/api/*`hop; the`VERCEL*TARGET_ENV` tier gate of ADR 0104 already splits preview/prod). Reversible — the API is a plain Docker image with no Railway-proprietary coupling (moves to any container PaaS), Vercel swaps for any Next host. Secrets in the platform stores (`@repo/config/env/*`is the contract), TLS platform-terminated, all stateful tiers EU-region, Redis + buckets non-PII-bearing by construction (IDs-only payloads). Migrations run as the **release phase** (ADR 0038, never at boot). CD + a **deployed smoke test** whose merge gate never assumes hosted-green until proven (deep-pass F3/G-02). Backups + a **PROVEN restore drill** (a backup isn't a backup until a restore is proven). Observability baseline reuses the ADR-0021 seam. Root-demo retirement (the skeleton users demo at`/`) + FIL org provisioning over the shipped org path (ADR 0055/0057) are designed here. **Design-only:** no cloud artifact ships — the Dockerfile / Railway·Vercel config / CD workflow / first deploy need Martin's cloud accounts (nothing to stand up or validate against from this box) + Docker (down; no prod Dockerfile exists yet); the root-demo retirement is a wider de-skeletoning touching the `users`route + i18n + the Playwright e2e specs (which need the full stack) and is deferred to its own slice | Accepted (design) | Topology + R0 spine designed; execution blocked on Martin's Railway/Vercel accounts + Docker; ADR is the executable spec |
| [0114](0114-design-canvas-adoption.md) | Adoption of the claude.ai/design canvas export at `design/` as the **design authority** for the product UI wave, and its reconciliation with the shipped design-system plumbing. The export's bundled design system is not an inbound artifact with an independent vocabulary — its token and component payload (`styles.css`, `_ds_bundle.css`, `_ds_bundle.js`) is byte-identical to the locally generated `ds-bundle/`, and `ds-bundle/.bundle-entry.mjs` re-exports absolute `packages/ui/src` paths, proving an **outbound** build of this repo that the canvas stored. Adoption therefore requires no token migration. What it does require is enforcement: the design-sync token input is `packages/ui/.ds-css/theme.css`, a hand-maintained copy of the canonical `tooling/tailwind-config/theme.css` that nothing keeps in sync, so a stale copy makes the `styleSha` ledger report "no change" and drift goes invisible — fixed by generating the copy from a tracked preflight rather than by a test (`.ds-css/` and `.design-sync/` are both git-excluded, so no fresh-clone assertion is available). Also decides the prerequisite slice the wave cannot start without: a spacing scale and rationalised type ramp (`theme.css` declares neither, so the canvas improvised 256 `gap:` / 171 `padding:` / 290 `fontSize:` literals over a 23-step half-pixel ramp), a derived interaction-state token layer (only `--color-copper-hover` exists today), and the **root-only dark-variant defect** — top-level `@variant dark` compiles to `:scope:where(.dark, .dark *)`, so token overrides match `html.dark` only while `dark:` utilities match any descendant, which is why `/brand-lab?theme=dark` (the ADR-0111 eyes-on route) renders half-flipped. Design owns IA, navigation, layout, flow presentation and interaction; it does not own domain truths, the settled brand tokens, or 3D scene contents. The shipped UI carries no design authority and is replaced surface by surface, preserving the engine wiring and data-fetching logic beneath it. Companion artifact: `design/README.md`, the implementation contract and running gap-and-decision log. Succeeds [0111](0111-design-system-scale-and-component-kit.md) in the design lineage | Accepted | Adopted; prerequisite slice (tokens + dark fix + sync preflight) is the next build, surfaces follow Configurator-first |
| [0115](0115-release-authored-wizard-steps.md) | The configurator renders the release's **own** wizard steps one-to-one, wrapped by three app-shell steps (Produkt · «release steps» · Barva a povrch · Souhrn). **Supersedes [0077](0077-camera-choreography-and-cz-wizard.md) in part** — that ADR's fixed five-step spine consumed `ui.steps` *positionally* (first authored step seeded Lokalita, all remaining ones were flattened into Konfigurace), which discarded the release's authored step identity even though `UiSpec` models it fully and validates it at publish (`ui.param.uncovered` / `ui.param.duplicate`). Two step models shipped side by side and disagreed: the standalone configurator flattened, while the in-project instance editor (`wizard.tsx`, reached from `/site/:projectId`) already rendered `ResolvedUiStep[]` faithfully — so the same release rendered differently in the two surfaces. They are now one model. Pure presentation: **no schema change, I3 untouched** — only which screen a parameter appears on. `UiStep` carries no camera or view hint, so ADR 0077's two kind-keyed presentation features get a **positional carry-over** rule: the first release-authored step takes the `hero` pose and the top-down `SitePlanSvg` (the old Lokalita treatment), every later one takes `detail`, and the shell steps keep their ADR 0077 poses. Rejected: flattening the choreography (drops the camera animation the brand grammar exists for), and widening `UiStep` with `camera?`/`view?` (structurally correct, but a `packages/model` change with publish-gate, validation and corpus impact — **deferred to its own ADR**, and it is the exit from the positional wart). Step nav keys on `${kind}:${id}`, never `id` alone: publish validation enforces step-id uniqueness only *within* a spec, so a release authoring a step called `produkt` would collide with the shell step and the nav would silently jump to the wrong step. Settles the step-model question `design/README.md` §8.2 left open as blocking for Phase 1 | Accepted | Shipped in the ADR 0114 Phase 1b configurator slice |
| [0116](0116-configurator-commercial-plumbing-and-surface.md) | The `/configurator` surface built against the `frames-v2` canvas, plus the commercial plumbing that turned out to be its prerequisite. **Not a reskin in two places.** (1) The canvas's cost / margin / margin-floor panel had no data behind it: `catalog-bundle.ts` parsed the whole `priceTableSchema` and kept `table` alone, `CatalogBundle.prices` was a bare `PriceTable`, and `deriveForUi` passed no `options`, so `result.costTotals`/`costMoney` were structurally always `undefined`. (2) The same omission carried a **live correctness defect** — `DeriveOptions.rounding` (ADR 0081) was threaded by `quotes.service.ts` at `issue`/`verify` but NOT by the client derive, which silently fell back to `DEFAULT_ROUNDING_POLICY`, so the price a rep read off the configurator could differ from the price the quote froze; `app/site/derive.ts` had the identical gap. Fixed by making pricing travel as one `ConfiguratorPricing` object (`table` + `cost` + `marginFloorPct` + `rounding`), **renamed** `prices`→`pricing` so every call site is a compile error rather than a shape that quietly still destructures, with `rounding` **required** — the engine's default is exactly how the defect stayed invisible. I3 untouched (the server was always correct; the client now agrees). The derive moves onto the shared ADR 0114 engine-worker transport per §7.6 (monotonic id, last-write-wins, 150 ms debounce, synchronous fallback), with the context-cache effects **declared before** the request effect and a `no-context` worker guard — two independent guards on one failure mode whose consequence is a plausible wrong price, not a crash. Cost/margin narrow to **admin-only** (`useCanSeeCost`), a tighter line than ADR 0056 price-blindness — screen hygiene, explicitly NOT a security boundary. The undesigned `Rozpad`/BOM view is settled: grouped by `part.category`, per-group subtotals, money columns **absent** (not masked) when price-blind. Deliberately NOT built, each because it would lead nowhere: the context bar's quote number / project name / save state / back link (standalone `/configurator` is not project-bound — the bar degrades honestly), *Povolit odchylku* (no deviation-ledger backend), the commercial panel's CTAs (saving already exists on the Souhrn step), and the `locked` step + *V1* badge (no data model). The §8.2 unguarded `RAL[ral].hex` crash is in the design export only — the app's `scene/finish.ts` has nine entries and a guarded fallback. The immersive frame + the §7.6 direct-manipulation loop follow as their own commit | Accepted | Shipped |
| [0117](0117-configurator-immersive-frame-and-direct-manipulation.md) | The `v2-IMM` immersive frame + the CORE_SPEC §7.6 direct-manipulation loop ADR 0116 deferred. **Immersive is a layout MODE** (a manipulation-store slice) that promotes the scene `<main>` to `fixed inset-0` and unmounts the banded chrome, but the `<main>` element is rendered in both branches so the **WebGL canvas never remounts** on toggle. The §7.6 loop lives in a **module-singleton zustand store** (crossing the R3F reconciler boundary like `useExplode`/`useSection`) plus an app-land bridge synced each render; the drag derives on the ADR 0116 worker through a new non-debounced `requestImmediate` (rAF cadence for drag, distinct from the 150 ms keystroke debounce), last-write-wins, **one `commit` on pointer-up**, clamped to the parameter's `range` domain. Corner handles + editable dimension pills edit the SAME parameter the form field does; **dimension bindings are positional** (first two visible `range` params = width/height — a heuristic pending a schema `dimensionRole`, recorded deviation). **Výběr selects + identifies but does NOT fabricate a parameter toolbar** — the release authors no part→parameter binding and §7.6/§5 forbid inventing one (recorded deviation); the frame's spacing stepper has nothing to address. Tool dock: Výběr/Kóty drive the store, Řez/Rozklad delegate to the existing slices, **Měřit/Otočit deferred** (Martin's scope call) as disabled affordances. Optimistic pre-derive scene-scale **deferred** — a naive scale double-counts against the live per-rAF derive; the handle/pill track the pointer live and the fast worker keeps the scene within a frame or two. The **adversarial review pass caught five focus/keyboard defects** the gate could not (controls inside the `ssr:false` canvas are jsdom-invisible), incl. two native-`disabled`-drops-focus regressions — all fixed with isolated-render tests. **Eyes-on (§12.1 item 6) PENDING** — credential-blocked this session; the capture harness is extended and ready | Accepted | Shipped (eyes-on pending) |
| [0118](0118-authenticated-app-shell.md) | The authenticated **app shell** (design §4), **superseding the CAR-12 top-bar `NavShell`** — one invariant role-filtered registry rendered at three densities (220 px labelled rail ≥1280, 68 px icon rail 768–1279, bottom tab bar + top app bar <768). `nav-registry.ts` rewritten to §4.1 tenant vocabulary + the §4.3 matrix (workshop loses the priced Poptávky/Nabídky/Katalog; Nastavení is always-on, reachable even by an org-less `role: null` session; Platforma is orthogonal to org role); `visibleNavEntries` stays the **single filter** and a new `isNavEntryActive` centralises active-state matching (own route + `activeMatch` section children). `nav-shell.tsx` is split into `SideRail`/`IconRail`/`TabBar`/`MobileTopBar` over a shared `NavRowLink`, toggled **purely by CSS breakpoints** (no JS media query → no hydration hazard, and only the visible nav is in the a11y tree). The shell **wraps** `{children}` (a rail beside content), retiring the configurator's hand-duplicated `h-[calc(100dvh-3.5rem)]` coupling → `h-full`; its content slot is a role-neutral `<div>`, **not a `<main>`** (every page owns its own `<main>` — the nested-landmark regression the adversarial pass caught). Bare passthrough (never null) on unauth/public/print routes preserves each page's `AuthGuard` fallback; the live §4.2 `/traveler` print-chrome bug is fixed; the mobile tab bar is suppressed where a surface owns a bottom action bar (`/configurator`) or the main group is empty (`role: null`). Recorded §11.2 deviations: Nastavení → `/account` (the `/settings` tabbed index is the 1c-2 slice), the §4.4 "avatar button" renders the account **initial** (no avatar-image model), and count pills are **absent** (the `GET /v1/me/nav-counts` endpoint is 1c-3; an empty pill is worse than none). The mandated adversarial review found **14 real defects** the green gate missed (nested `<main>`, no `focus-visible` ring on any of the four controls, broken active-state on the framed `/projects`·`/site` Nabídky routes, an empty tab-bar flash, and a test that asserted all rails present at once and so never pinned the breakpoint-collapse invariant) — all fixed. Eyes-on cleared in both themes at all six ship-bar widths (no horizontal scroll, no page errors); a **capture-harness dark-mode race** was found and fixed (a context-per-theme `colorScheme` replaces the racy localStorage-seed + `classList.add`, which silently produced LIGHT desktop "dark" captures). No schema change; I1/I3/I5 untouched | Accepted | Shipped (1c-1); `/settings` index (1c-2) + nav-counts pills (1c-3) queued |
| [0119](0119-orders-surface-reskin.md) | The **Zakázky (orders) surface reskin** — Phase 2's first surface (design §11.1), and the ADR that establishes the reskin-wave method + the two per-surface fixes every later surface repeats. The `/orders` list is reskinned to the canvas `o-LIST` look (a bare accessible `<table>` — uppercase muted heads, hairline rows, per-row hover, one stretched-link anchor per row = full-row click + single tab stop — replacing the Panel-per-row list, in the settings-layout idiom, with skeleton/`EmptyState`/`role="alert"` states and the role-split routing unchanged); the `/orders/[id]/production` detail is reskinned chrome-only (frame + per-branch min-h fix + registry-derived "Zakázky › {orderNumber}" breadcrumb), leaving the shared price-blind `ProductionView` untouched. Governing principle: **contract-honesty** — the canvas is the LOOK authority, never a data authority, so where it overdraws (`orderSchema` is thin: 4 statuses, no customer/product/value/due; no aggregate endpoint; the detail route is the price-blind workshop view) the honest surface wins and the divergence is recorded. Recorded §11.2 deviations: the 4 KPI tiles omitted (no honest aggregate under keyset), the customer/product/value/due columns omitted (thin schema, no cross-module join), the Export/"Nová zakázka" actions omitted (no endpoint/flow), the ADR 0111 status tones kept over the canvas info-blue (deliberate single-accent), the `o-DETAIL` six-stage timeline/money-strip/customer tabs NOT ported (price-blind route by construction), the breadcrumb order-number vs the shared `ProductionView` H1 quote-number split (H1 can't change — reused by the priced N-1 `/quotes` surface), and the **per-branch** min-h fix (authed `<main>` drops `min-h-screen`; the bare `AuthGuard` fallback keeps it — the blanket codemod ADR 0118 deferred is unsafe in the unauth branch). The mercata method's adversarial pass found 1 real defect the green gate missed (breadcrumb showed the quote number) — fixed via a thin role-independent price-blind `GET /v1/orders/:id`. No schema change; I1/I3/I5 untouched; price-blindness net preserved + a detail absence assertion added; eyes-on cleared both themes ×6 widths (no horizontal scroll, dark resolves) | Accepted | Shipped |
| [0120](0120-quotes-surface-reskin.md) | The **Nabídky (quotes) surface reskin** — Phase 2 surface 2. The `/quotes` list is reskinned to the ADR 0119 orders o-LIST table language (§6 "Zakázky list language" — no quotes board exists), honest columns document-number/status(effectiveStatus)/total/validity with the money column behind a `quote.total !== null` price gate. The `/quotes/[id]` detail is the §3.2 quote DOCUMENT in the Nabídka document language (`design/Perimetra Nabidka.html`/`frames-quote.jsx`): the titleband (eyebrow + number + meta, `border-b-2 border-primary`), the §92e/DPH Daňový doklad + the aggregated site BOM sharing the `.price` table treatment (hairline rows, a `border-t-2 border-primary` spotlight total, gross in copper), the buyer share link, and the Ověřitelná-nabídka reproducibility trust panel — the existing working behaviour POLISHED, not rebuilt. Gap-fills added: the revision-lineage indicator (links by ID — `quoteSummarySchema` carries only the UUIDs, not the sibling's document number) and the aggregated site BOM with per-instance provenance (SESTAVY). **The BOM is price-blind by a two-boundary coinciding gate**: the server `blindSnapshot` (`quotes.service.ts:393`) drops `totalPriceMoney`/`money`/`totals` and nulls `total` for workshop (money not on the wire), AND the client renders the BOM Cena `<th>`+`<td>` only behind `quote.total !== null` — component/qty/category/provenance always, money never unpriced. Also fixed `text-green-700`→`text-success` (§12.2 no-raw-color) and carried the per-branch min-h fix. The mercata pass (spec → i18n-seed → disjoint builders → gate → adversarial review hunting a workshop price leak → skeptic → fix) found **0** defects; a real `total:null` blind-snapshot fixture was added so the absence assertions run against a genuine price-blind row. No schema change; I1/I3/I5 untouched; eyes-on cleared both themes ×6 widths (list + full §3.2 detail incl. the golden 157 168,72 Kč gross + the 19-line site BOM) | Accepted | Shipped |
| [0121](0121-projects-customers-surface-reskin.md) | The **Projekty (projects) + Zákazníci (customers) surface reskin** — Phase 2 surfaces 3+4, shipped together as one parallel wave (one Workflow: sequential i18n seed → three disjoint builders → cold gate → adversarial review → skeptic → fix; gate passed first try, review found **0** defects across three opus dimensions — PII/role/behaviour, design/a11y, i18n/tests). Both are the least-styled surfaces (projects the oldest scaffolding idiom; customers' `CustomerForm` the "functional-minimal, out of scope per CAR-23" placeholder). A structural asymmetry drove the design (§4.2/§5): **`/projects` is list-only** (its detail `/site/[projectId]` is fenced OUT), **`/customers` is list + detail**. `/projects` list → o-LIST table (Název stretched-link→`/site/:id` · Popis · Stav badge · Vytvořeno · **a trailing actions cell** Archivovat/Smazat layered above the stretched link with `relative z-10` — the divergence from the pure-nav orders o-LIST, because projects has no in-surface detail to host the actions), settings-layout + per-branch min-h, kit create form. `/customers` list → o-LIST table (Zákazník→`/customers/:id` · IČO · Město · Stav), **pure-nav** (archive/restore live on the detail), the deferred search restyled to a kit `Input`, role gate + 404-no-oracle untouched. `/customers/[id]` detail → the quotes-detail language, lighter: titleband (back-link + `DisplayLabel` + status badge + archive/restore) over the shared `CustomerForm` regrouped into Identifikace/Kontakt/Adresa/Poznámka `Panel`s on kit `Field`/`Input`/`Switch`/`Textarea`, ARES prefill + VIES badge preserved, create/edit branch byte-identical. **Load-bearing engineering finding — the shared o-LIST horizontal-overflow fix:** eyes-on caught `/projects` overflowing the body at 390 px (`scrollWidth` 660>390, both themes) — the stretched-link `after:absolute after:inset-0` pseudo, positioned against the `relative` `<tr>`, is NOT clipped by the non-positioned `overflow-x-auto` wrapper when the table's nowrap actions cell pushes min-content past the viewport, so it escapes to the document. Fix = `relative min-w-0` on the scroll wrapper (`min-w-0` lets the flex-item shrink and scroll; `relative` makes it the clip/containing-block boundary), proven by measurement (`scrollWidth` 390 after). Hardened across **all four** o-LIST surfaces (orders/quotes never exposed it only because their tables fit under 390 — a visual no-op there, latent bug fixed). Recorded §11.2 deviations: projects inline-actions vs pure-nav split, thin-schema columns with `?? "—"` fallbacks + empty Popis cell, no KPI/aggregates, ADR-0111 status tones, no in-page tab strip (§4.2 IA owned by the ADR 0118 app-shell nav), `/site` detail stays OUT, the CustomerForm CAR-23 placeholder retired. No schema change; no backend behaviour changed; per-rep scope/PII/workshop-403 preserved; eyes-on cleared both themes ×6 widths (no horizontal scroll after the fix, dark resolves); reusable `capture-projects-customers.mjs` added. Full gate green (check-types, lint, web vitest 544, build, knip) | Accepted | Shipped |
| [1000](1000-adr-numbering-reserved-band.md) | **Skeleton-authored** (drained verbatim at its upstream number — see the band note below). ADR numbering reserves ≥1000 for skeleton-authored decisions; 0001–0999 stays derived-project territory. Perimetra's own next free number is 0118; skeleton ADRs 0045–0049 remain grandfathered and are the ones renumbered into 0099/0104–0107 | Accepted | Implemented (band adopted;`scripts/create-project`no longer seeds a stamp's numbering from the reserved band) |
| [1001](1001-login-form-post-method.md) | **Skeleton-authored** (drained verbatim). The login form carries an explicit`method="post"`: a form with no `method`defaults to GET, and a submit landing before the client island hydrates falls back to the native, unhandled submit — serialising the plaintext password into the request URL, and from there into browser history, the`Referer`header, and every proxy/CDN/origin access log. Orthogonal to TLS (HTTPS protects the wire, not the URL's persistence). The regression test asserts the rendered`method`attribute, never the`onSubmit` handler, because the handler is precisely what is absent in the window the fix protects. Applies as a class rule to every credential-bearing form | Accepted | Implemented (`apps/web/app/login/login-form.tsx`; RED/GREEN-proven test — without the attribute the assertion reads `expected null to be 'post'`) |
| [1002](1002-turbo-test-keeps-caret-build-no-caret-test.md) | **Skeleton-authored** (drained verbatim). The `test`task keeps`dependsOn: ["^build"]`and does NOT add`^test`: turbo materializes a `<NONEXISTENT>`placeholder`#build`node for every script-less package, hashing its full source, so`^build`already invalidates a consumer's test cache on a source-only package's change. Adding`^test`would serialize cold-cache runs for zero correctness gain | Accepted | Implemented (no`turbo.json`change; re-verified against perimetra's own graph on turbo 2.9.17 —`@repo/api#build`is a`<NONEXISTENT>`node over 33 inputs with`web#test`among its dependents) |
| [1003](1003-ci-cadence-fast-gates-push-heavy-scheduled.md) | **Skeleton-authored** (drained verbatim). CI cadence: the fast gates (lint, type-check, test, knip, gitleaks) run on every trigger; the heavy jobs (build, docker-build, trivy, integration, e2e-web, smoke-e2e, audit) are kept off a bare push to`main`, running on PR + a weekly schedule + manual dispatch, and `audit`is bounded by`timeout-minutes: 12`| Accepted | Partial — perimetra reaches the same cadence by TRIGGER SET, not by job guards: it has never had a`push:`trigger, so no job runs on a bare push and the ADR's per-job`if: github.event*name != 'push'`would be inert here. Adopted: the event-scoped`concurrency`group (it fixes a live defect — a manual dispatch could cancel the weekly run, which shares its`github.ref`) and the bounded `audit`timeout. Not adopted: the`push:`trigger and the per-job guards.`ci.yml`records the guards as a prerequisite for ever reinstating`push:`|
| [1004](1004-gdpr-export-core-user-row-scope.md) | **Skeleton-authored** (drained).`exportUser()`(Art. 20) gains a built-in core step symmetric with the existing erase-side one: it emits the Better Auth`user`row under a FAIL-CLOSED allow-list of exactly`id/name/email/emailVerified/image/locale/createdAt/updatedAt`— excluding the admin() moderation flags and never reading`account`/`session`. Closes the asymmetry where erasure anonymised the core identity row but the export omitted it. Amends [0040](0040-gdpr-privacy-audit.md) (export half only); a contract test pins the exact emitted key set | Accepted (HQ-ruled, ratify queued) | Implemented |
| [1005](1005-form-method-lint-rule.md) | **Skeleton-authored** (drained). Local ESLint rule `local/no-form-missing-method-with-sensitive-input`flags any`<form>`with a secret/PII-bearing named input (a literal`type="password"`, a `name="…"`, or a `{...register("…")}`spread over a strong-substring/exact-match sensitive set) that lacks`method="post"`— the pre-hydration native-GET leak class [1001](1001-login-form-post-method.md) opened, widened past`type="password"`to all PII. Wired into`next.js`+`react-internal.js`at`"warn"`(CI-blocking under`--max-warnings 0`) | Accepted (HQ-ruled, ratify queued) | Implemented (`tooling/eslint/local/`; perimetra's `create-user-form.tsx`already carried`method="post"`) |
| [1006](1006-remove-jwt-era-cookie-constants.md) | **Skeleton-authored** (drained). Remove the dead JWT-era `ACCESS_TOKEN_COOKIE`/`REFRESH_TOKEN_COOKIE`/`ACCESS_TOKEN_TTL_MS`from`@repo/config`(pre-0033 short-lived-JWT model; zero runtime consumers — the "api-mocks needs them" justification went stale when that mock moved to Better Auth sessions). Supersedes the identity-constant clause of [0010](0010-ui-state-zustand-store-package.md); the auth model is [0033](0033-better-auth.md) | Accepted (HQ-ruled, ratify queued) | Implemented |
| [1007](1007-ci-self-hosted-runner-default.md) | **Skeleton-authored** (drained, adapted). CI defaults to a self-hosted runner via`runs-on: ${{ vars.CI_RUNNER \|\| 'ubuntu-latest' }}`(the portfolio-wide fix for the billing-blocked account; inert until`CI_RUNNER`is set) + fork-PR fence on every job + workflow-level`permissions: contents: read`+ gitleaks switched from`gitleaks-action@v3`to the pinned MIT CLI 8.24.3. Amends [1003](1003-ci-cadence-fast-gates-push-heavy-scheduled.md) (runner half). PERIMETRA: the fork fence is standalone (no`!= push` guards exist here — cadence is by trigger set), and it is adopted in FULL, unlike 1003's partial adoption | Accepted (HQ-ruled, ratify queued) | Implemented (`ci.yml` all 11 jobs; the runner install is HQ-owned/parked) |
| [1008](1008-public-signup-fail-closed-in-production.md) | **Skeleton-authored** (drained by content; ADR re-authored, not cherry-picked). Public self-serve sign-up (`/api/auth/sign-up/email`) is fail-closed in production: `emailAndPassword.disableSignUp = !allowSelfSignUp(env)`, where `allowSelfSignUp = NODE_ENV !== "production" \|\| AUTH_SELF_SIGN_UP`. A denylist on production (open everywhere else, so `NODE_ENV=test`keeps`auth.itest.ts`working), keyed on the same`NODE_ENV=production`signal`assertProductionSecrets`trusts;`AUTH_SELF_SIGN_UP`is a string-enum→boolean env, not`z.coerce.boolean`. Perimetra-specific: an anonymous sign-up auto-provisions a tenant (ADR 0055/0063), so this closes an unauthenticated org-creation surface — `admin.createUser` + invite-accept remain the account paths | Accepted (HQ-ruled, ratify queued) | Implemented (`auth.instance.ts`; `allowSelfSignUp`+ resolved`disableSignUp`unit-tested) |
| [1009](1009-sentry-request-pii-scrub.md) | **Skeleton-authored** (channel-A drain of`63fa132`, by content; ports anyora ADR 0070). The Sentry `beforeSend` scrubber (`sentry/init.ts`) is the terminal guarantee for five more request-PII surfaces the key-scrub cannot reach: it drops `event.request.data`(raw body blob), strips the querystring off`event.request.url`(reusing`stripQueryString`from the pino redaction module — one definition of "cut the querystring"), drops`event.request.query_string`, deletes the `referer`/`referrer`headers, and walks`event.breadcrumbs`dropping`http.query`/`http.fragment`+ stripping`data.url`. Source-level `maxRequestBodySize` rejected as version-fragile (`@sentry/node`10.57 does not expose it on the default`"Http"` integration). Amends the scrubbing half of [0040](0040-gdpr-privacy-audit.md). Logging/telemetry only — no request/RLS/migration surface | Accepted (HQ-ruled, ratify queued) | Implemented (`sentry/init.ts`; five-surface + no-PII-survives + absent-breadcrumb cases in `init.test.ts`) |
| [1010](1010-erasure-plumbing-generalized.md) | **Skeleton-authored** (channel-A drain of `63fa132`, by content; ports anyora ADRs 0068 + 0067, adapted). `PurgeHook.purgeUser`now returns a`PurgeOutcome` (`purged`/`documented`/`skipped`— no`failed`: a hard failure THROWS → retries → DLQs, the ban-purge escalation); the processor collects them into a read-model recorded in the `privacy.erase`audit`diff`AND the BullMQ`job.returnvalue`— **adaptation**: no`erasure_request`table here (unlike anyora), so no new schema/migration. Plus an optional`PrivacyHandler.finalizeErasure?()`second-pass seam run after the whole`eraseUser`fan-out, before core (generic seam only; anyora's CareGrants sweep not ported,`ProjectsPrivacyHandler` unchanged). Item 2d (force-rls) N/A — [0107](0107-tenancy-app-level-scoping-not-rls.md) rejected RLS. Extends the fan-out of [0040](0040-gdpr-privacy-audit.md) | Accepted (HQ-ruled, ratify queued) | Implemented (`privacy.processor.ts`/`privacy.tokens.ts`/`purge/*.ts`; outcome + finalize-order + escalation unit tests) |
| [1011](1011-client-sentry-url-query-scrub.md) | **Skeleton-authored** (channel-A drain of `de34dd2`, by content; ADR re-authored). The client/mobile Sentry scrubber (`packages/telemetry/src/scrub.ts`) gains a deny-BY-DEFAULT URL-query strip on top of its value-shape + `pii()`-key deny-lists: an arbitrary `?search=<surname>`param (no matchable shape, key not a`pii()`column) shipped in a fetch breadcrumb, span`url.full`/`description`, or transaction URL. URL-bearing fields now keep `scheme://host/path` and drop query+fragment (`url`/`url.full`/`referer`+ span`http.request.header.referer`); bare-query fields (`query*string`/`url.query`/`search`) drop wholesale; `to`/`from`strip only when path-shaped; an embedded absolute URL in free text is query-stripped in`redactString`; `scrubSpan`moved into`scrub.ts`so every rule is single-homed. The client mirror of [1009](1009-sentry-request-pii-scrub.md) (server), which named this follow-up. Perimetra's`SENSITIVE_KEYS`superset (odběratel`ico`/`dic`/…, ADR 0071/0082) unchanged — additive. Telemetry-only. Extends [0021](0021-telemetry-observability-package.md) | Accepted (HQ-ruled, ratify queued) | Implemented (`scrub.ts`; URL-strip + referer/span-desc/nested-data/embedded-URL/`to`-`from`-gate + SQL-`?`non-truncation cases in`scrub.test.ts`/`sentry-options.test.ts`) |
| [1012](1012-e2e-web-port-multiseat.md) | **Skeleton-authored** (channel-A drain of `de34dd2`, by content; ADR re-authored). The mock-mode web E2E port (`apps/web/playwright.config.ts`) becomes `WEB_PORT`-configurable: it hard-coded `PORT = 3000`with`reuseExistingServer: !CI`, so on a multi-seat box a second repo's `test:e2e`silently drove the first repo's app (identical UI → false-green). PORT now derives from`WEB_PORT`(the knob`apps/web`'s dev script already reads) and is threaded into `webServer.env`, so wait-URL and bound port can't drift; a new `e2e/README.md`documents the trap + the`ss`/`/proc`port-ownership proof recipe. Default (unset) stays :3000. Perimetra delta: the real-stack`playwright.smoke.config.ts`was already multi-seat-safe (own`SMOKE_WEB_PORT`, no reuse) — left as-is. Test-infra only. Extends [0025](0025-web-e2e-playwright-shared-vitest-config.md) | Accepted (HQ-ruled, ratify queued) | Implemented (`playwright.config.ts`; `e2e/README.md`) |
| [1013](1013-posthog-analytics-url-scrub.md) | **Skeleton-authored** (channel-A drain of `2f83b8d`, by content; ADR re-authored). Closes the second client sink, named as owed by [1011](1011-client-sentry-url-query-scrub.md): `posthog-js`autocaptures`$pageview`/`$autocapture`internally and attaches the full URL — query included — as`$current_url`/`$referrer`, never passing through the telemetry analytics adapter, so an arbitrary `?search=<surname>`shipped in the clear on every page load. A new SDK-free`sanitizeAnalyticsProperties`is wired into`posthog.init`'s `before_send`; because `posthog.init`lives in`@repo/flags`and the DAG forbids`flags → telemetry`, it is injected from the app composition root (`apps/web/app/providers.tsx`), mirroring the identity/token pattern. Deliberately URL-query-only, not the Sentry-style value-shape walk: `identify`ships email/username by design and a blind pass would corrupt PostHog's structural ids.`$elements_chain` gets a dedicated in-place `href` scrub; `$snapshot`replay batches pass through untouched (rrweb owns its own masking). Also closes two gaps in the 1011 walk: no`transaction`-key rule, and the embedded-URL strip was http(s)-only (missing `ws(s)`/protocol-relative), with both passes' query tails now bounded carrier-aware. **Diverges from upstream on two rules** whose upstream form the drain review proved leaks: the tail bound `[^\s"'<>]*`stops at the first quote ANYWHERE (so`?token="abc"&surname=…`keeps the surname — worse than perimetra's shipped`\S*`), and the `$elements_chain` href extent ignores posthog-js's `\"` escaping. Perimetra terminates the query only on a delimiter that CLOSES the carrier (decided by what follows it), and matches to the first UNESCAPED quote; regression-tested, and owed upstream. Extends [0021](0021-telemetry-observability-package.md) | Accepted (HQ-ruled, ratify queued) | Implemented (`packages/telemetry/src/analytics-scrub.ts`; `packages/flags/src/web.tsx` `sanitizeProperties` prop; `packages/telemetry/src/scrub.ts` transaction + ws/protocol-relative rules) |
| [1014](1014-compose-project-name-isolation.md) | **Skeleton-authored** (channel-A drain of `2f83b8d`, by content; ADR re-authored). `docker/compose.yaml`'s committed `name:` moves from the bare literal `fullstack-skeleton` to `${COMPOSE_PROJECT_NAME:-perimetra}`. Perimetra inherited the un-fixed literal verbatim at its 2026-06-12 stamp, so a fresh clone with no gitignored `docker/.env`fell back to sharing the`fullstack-skeleton*\*`Docker volume namespace with the skeleton itself and every sibling stamp — isolation existed only on boxes carrying an untracked file. The fix is retroactive for perimetra, not just future stamps.`create-project`'s guarded rewrite is ported too (dormant here — perimetra is already stamped — but kept byte-comparable and live for any future spin-off; template drift fails the stamp loudly rather than silently sharing a namespace). Infra config only, outside the automated gate: verified by `docker compose config`reporting`name: perimetra` with no env var set. Extends [0042](0042-template-lifecycle.md) | Accepted (HQ-ruled, ratify queued) | Implemented (`docker/compose.yaml`; `scripts/create-project/index.mjs`) |
| [1016](1016-url-scrub-key-list-misses-bare-path-and-fragment.md) | **Skeleton-authored** (channel-A drain of `2012000`, by content; ADR re-authored). One decision over two key lists in `packages/telemetry/src/scrub.ts`: a field whose value is URL-DERIVED but whose SHAPE defeats every value-level defence, so only its key NAME can catch it — and the name was missing. `http.target`joins`URL_KEYS`: it is path+query (`/clients?search=Nováková`), not an absolute URL, so `stripEmbeddedUrlQueries`(which needs`://`or a dotted`//host`) is a no-op on it and the querystring survived verbatim; Next.js sets it unconditionally on the `BaseServer.handleRequest`root span (on`NextVanillaSpanAllowlist`, so no `NEXT_OTEL_VERBOSE`needed) and Sentry's`httpServerSpansIntegration`sets it too, neither gated on`sendDefaultPii`, and the OTel bridge maps it onto `contexts.trace.data`/`spans[].data`. `url.fragment`/`http.fragment`join`QUERY_ONLY_KEYS`(dropped wholesale — no path worth keeping). Rejects the tempting "the twins ride together so a covered key shields its sibling" rationale as FALSE: the two SDK writes are independently guarded on`parsedUrl.search`/`parsedUrl.hash`, so `/path#email=…` emits the fragment alone (pinned by a test). Server-tracing only (`tracesSampleRate > 0`); error events carry no `data`. Extends [1011](1011-client-sentry-url-query-scrub.md) | Accepted (HQ-ruled, ratify queued) | Implemented (`scrub.ts` `URL_KEYS`/`QUERY_ONLY_KEYS`; `scrub.test.ts`http.target + fragment-twin + fragment-without-query cases) |
| [1017](1017-scrub-key-lists-miss-containers-and-attribute-forms.md) | **Skeleton-authored** (channel-A drain of`2012000`, by content; ADR re-authored). Closes the two STRUCTURAL reasons a key name goes missing — and both live on the ERROR path, which [1016](1016-url-scrub-key-list-misses-bare-path-and-fragment.md) did not reach, so `tracesSampleRate: 0`was no protection. (a) CONTAINER keys:`event.request.cookies`is the parsed cookie jar, filled by the default-on`requestDataIntegration`with NO event-path filtering; the anchored singular`cookie`never matched the plural, so the walk descended into the jar and tested each COOKIE NAME — and`**Host-auth_session_token`matches no anchored token rule while its two-segment value matches no shape (the JWT pattern needs three), so an httpOnly session token shipped verbatim beside the`[Filtered]`copy of the same secret.`cookies`now drops wholesale.`event.request.data`(raw unparsed body,`include.data`hardcoded true, 10KB default) drops via a new`REQUEST_SCOPED_SENSITIVE_KEYS` gated on one level of parent context (`underRequest`) — scoped rather than global because `spans[].data`/`contexts.trace.data`are the attribute bags every URL rule operates on, and a global`data`rule would blind 1016's own coverage. (b) ATTRIBUTE-NAMESPACED forms of names the list already owns: a new`SENSITIVE_ATTRIBUTE_KEYS`covers`http.client_ip`/`net.peer.ip`/`client.address`/`http.user_agent`/`user_agent.original`/`http.{request,response}.header.user_agent`/`http.request.body.data`/the `http.{request,response}.header.{cookie,set_cookie}`family — written as plain literals in the SAME`startSpan`bag`http.target`was read out of.`isSensitiveKey()`ORs both lists at both dispatch sites so they cannot drift; kept a separate const so`scrub.pii-contract.test.ts`'s column mirror stays readable. Brings [1009](1009-sentry-request-pii-scrub.md)'s api-side wholesale deletes to the SHARED scrubber (`apps/web` was the weak binding). Extends [1011](1011-client-sentry-url-query-scrub.md)/[1016](1016-url-scrub-key-list-misses-bare-path-and-fragment.md), mirrors [0040](0040-gdpr-privacy-audit.md) | Accepted (HQ-ruled, ratify queued) | Implemented (`scrub.ts` `SENSITIVE_KEYS`/`SENSITIVE_ATTRIBUTE_KEYS`/`isSensitiveKey`/`underRequest`; `apps/api/src/sentry/init.ts`comment;`scrub.test.ts`cookie-jar + request-body + attribute-form + bag-walkability cases) |
| [1018](1018-elements-chain-quote-alternation.md) | **Skeleton-authored** (channel-A drain of`2012000`, by content; ADR re-authored). Corrects the `$elements_chain`rule of [1013](1013-posthog-analytics-url-scrub.md) on two counts. LEAK: the global`/((?:attr**)?href=")([^"]*)(")/gi`infers the value's START from the literal bytes`href="`, and those are PLANTABLE inside any attribute value ending in `href=`— needing no quote, so producing no`\"`for the ambiguity guard to see; posthog-js sorts attributes with`localeCompare`, putting `text`LAST, immediately before the ancestor's`attr\_\_href`, so a link label "Paste the value after href=" opens a bogus match whose closing group IS the real href's opening quote and the token ships. Exactly the class 1013 generalised and declared closed — the fix had moved the inference from the value's END to its START and left it just as plantable. Now the extent is not inferred AT ALL: under the no-`\"`precondition quotes alternate exactly, so`split('"')` puts structure at even indices and values at odd ones, and an odd segment is an href iff the structure segment before it ends in an href attribute NAME (`CHAIN_HREF_NAME_TAIL`) — a position no value can occupy. OVER-REDACTION: the ambiguity drop was not gated on the chain containing an href at all, so one straight quote in a Czech UI label (`Smazat "Faktura 42"`) nuked the whole element tree for zero redaction gain; now gated on `CHAIN_HAS_HREF`. Also corrects the `/i`mechanism (HTML tokenization lowercases attribute names UNCONDITIONALLY —`HREF=`is reachable via`setAttribute` on an SVG-namespace element and via XHTML, not via foreign content) and documents the boundary class in both directions (`xlink:href`wanted; an Alpine binding expression truncated at its ternary`?` is the accepted cost). Extends [1013](1013-posthog-analytics-url-scrub.md) | Accepted (HQ-ruled, ratify queued) | Implemented (`analytics-scrub.ts` `scrubElementsChain`/`CHAIN_HAS_HREF`/`CHAIN_HREF_NAME_TAIL`; `analytics-scrub.test.ts` planted-`href=`+ href-less-ambiguous + odd-quote + namespaced-name + case-insensitive-gate cases) |
| [1019](1019-analytics-array-key-credential-vocabulary-and-net-host-ip.md) | **Skeleton-authored** (channel-A drain of`2012000`, by content; ADR re-authored). Three closures. (a) `scrubEntry`'s ARRAY branch passed `""` instead of the property key, disarming every key-gated branch — invisible for an array of OBJECTS (`$elements`restores real keys) but a live leak for an array of STRINGS:`href: ["/clients?search=Novakova"]`kept its query while the byte-identical SCALAR was stripped, and this sink runs no value-shape pass so nothing else catches it. Now carries the key, matching`scrub.ts`'s `scrubUrlValue`. (b) `SENSITIVE_KEYS`regains the credential vocabulary the`pii()`-column-mirror framing wrongly excluded (`tel`, `iban`, `bank_account`, `ssn`/`national_id`, `session_id`, `rc`, `phone_number`) — the list ALWAYS carried non-column entries (`password`, `token`, `rodne_cislo`), so "not a column" was never its membership test; `@repo/validators/primitives/cz.ts`mints`bankAccount`/`iban`a few lines from the rodné-číslo validator this module's header cites as its reason to exist, and no`STRING_PATTERN`matches any of these values. Bare`session`DELIBERATELY excluded (unlike web-native): it is a Better Auth TABLE whose`ip_address`/`user_agent`are individually registered`pii()`columns, and a container rule would hide them behind one`[Filtered]`and blind the ADR 0040 mirror. (c)`net.host.ip`RETIRED from`SENSITIVE_ATTRIBUTE_KEYS`— [1017](1017-scrub-key-lists-miss-containers-and-attribute-forms.md)'s direction-ambiguity rationale is refuted by the installed bundle: the attribute has exactly two writers and both are SERVER spans assigning`localAddress`(one via the`SEMATTRS_NET_HOST_IP`CONSTANT, invisible to a literal grep), while the client-span emitter writes only `net.peer.*`. It is the server's own address, same category as`server_name`.`net.peer.ip` stays. Extends [1017](1017-scrub-key-lists-miss-containers-and-attribute-forms.md)/[1018](1018-elements-chain-quote-alternation.md) | Accepted (HQ-ruled, ratify queued) | Implemented (`analytics-scrub.ts`array branch;`scrub.ts` `SENSITIVE_KEYS`/`SENSITIVE_ATTRIBUTE_KEYS`; `scrub.test.ts`vocabulary + anchoring +`net.host.*`+ four-cookie-arm cases,`analytics-scrub.test.ts`array cases) |
| [1020](1020-elements-chain-parity-must-be-verified.md) | **Perimetra-authored** (found by adversarial review of this repo's own`2012000`drain, NOT inherited — **owed upstream** to fullstack-skeleton and web-native-skeleton, which both carry the identical leak). Corrects the soundness argument of [1018](1018-elements-chain-quote-alternation.md): "with no`\"`anywhere, every`\"`IS a real delimiter" is FALSE, and the failure is TOTAL, not partial. It only rules out quotes the ESCAPER produced — and`posthog-js` escapes attribute keys/values (`autocapture-utils.js:603`) and strips quotes from class names (`:582`) but concatenates `element.tag*name` RAW (`:574-575`) from `elem.tagName.toLowerCase()`. The HTML tokenizer's tag-name state treats `\"`as an "anything else" code point and APPENDS it, so`<span\"x>`parses to localName`span\"x`, injecting a bare quote with NO backslash: `CHAIN_HAS_AMBIGUOUS_ESCAPE`never fires, split parity shifts by one, every href value lands on an EVEN index and is read as structure — the chain passed through BYTE-IDENTICAL (reproduced by driving the REAL posthog-js serializer over jsdom: 4 surviving copies of`search=Novakova&rc=7001011234`; the control with a well-formed tag name scrubbed to `/clients`). An odd-quote-COUNT check is NOT sufficient (verified: two injections restore an even count and still leak the first href). Fix: VERIFY the parity against the chain grammar before relying on it — every even-index segment but the last must end in `=` — and fall back to the existing ambiguous-case policy (drop if href present, else keep) when it does not. Every ADR 1018 behaviour is preserved and pinned (planted-`href=` label, truncated odd-quote chain, href-less ambiguous label, structure-only chains). The generalisation, third time at the right level: **a precondition about who ESCAPED a byte is only as strong as the weakest field the producer concatenates unescaped** — verify the structural assumption against the data, don't infer it. Extends [1018](1018-elements-chain-quote-alternation.md)/[1013](1013-posthog-analytics-url-scrub.md) | Accepted (HQ-ruled, ratify queued) | Implemented (`analytics-scrub.ts` `CHAIN_STRUCTURE_SEGMENT_TAIL`+ the pre-loop grammar check;`analytics-scrub.test.ts`tag_name-injection + even-count-injection + behaviour-preservation cases) |
| [1021](1021-api-url-https-gate-keys-on-loopback-not-node-env.md) | **Perimetra-authored** (surfaced by this session's own`git push`— the pre-push hook's`check-types`failed on a tree whose full gate had just been run green by hand; **owed upstream**, the rule is skeleton-authored so every derived repo carries it). The`API_URL`https-egress rule was gated on`NODE_ENV`, which describes the BUILD and never the NETWORK PATH — so it was wrong in BOTH directions. **Too tight:** `next typegen`/`next build`set`NODE_ENV=production` themselves, so the refinement rejected the project's OWN documented local config (`.env.example`suggests`API_URL=http://localhost:4000`) and `web:check-types`could not pass — it only ever appeared to pass via a turbo CACHE HIT whose entries were populated by`SKIP_ENV_VALIDATION=1`runs (which disable the refinement outright); the pre-push hook sets no such var, so **the hook gated on cache state rather than correctness**, and CI sets the identical http localhost and rode the same condition. The repo had adapted to the SYMPTOM — the "gate honestly with`SKIP_ENV_VALIDATION=1`" operating note IS the workaround, and it hid the defect by making the failure read as a known quirk. **Too loose (the security half):** whenever `NODE_ENV` WAS development, http was permitted to ANY host, so a dev box pointed at a shared staging backend (`http://staging.internal:4000`) forwarded real credentials in plaintext over a real network — the exact exposure the rule exists to stop; the dangerous and the safe case sat on the same side of the test. Fix: gate on the HOST's **loopback-ness** (`isLoopbackOrigin`) — loopback traffic never reaches a wire — and require https everywhere else in EVERY `NODE_ENV`, i.e. a net TIGHTENING. The helper is conservative because it GRANTS an exemption: `localhost` + RFC 6761 `*.localhost`, the whole `127.0.0.0/8`with every octet range-checked (a`^127\.`prefix test would admit the HOSTNAME`127.0.0.1.evil.com`), bracketed IPv6 `[::1]`, and `false`on an unparseable URL.`check-types`+`build`now pass BARE and cache-busted, so`SKIP_ENV_VALIDATION=1` is retired as a routine gate workaround. Generalisation: **a security predicate must test the property its own rationale names** — gate on the thing, not on an ambient proxy for it; and a gate that a cache can satisfy is not yet a gate. Amends [0018](0018-bff-route-handler-and-shared-mocks.md) | Accepted (HQ-ruled, ratify queued) | Implemented (`packages/config/src/env/web.ts`; `web.test.ts`loopback-under-production + remote-under-development + loopback-spelling and near-miss-hostname tables) |
| [1022](1022-elements-chain-non-href-segments.md) | **Skeleton-authored** (channel-A drain of`aa2c0c8`, by content; body copied verbatim per the reserved-band rule below, so its first-person "paid back upstream" voice is the SKELETON's — from perimetra this is an inbound drain, and nothing here is owed up). `$elements_chain` scrubs EVERY value segment, not only the `href` ones. posthog-js serializes every attribute of a non-sensitive element (`attr__src`, `attr__value`, `attr__data-*`) and folds the clicked element's `text` in too, so the chain carried URLs under names the href rule never matched — while the BYTE-IDENTICAL value under `$elements`/`$el_text` was stripped by the generic pass in the SAME call. `$elements_chain`is the field ingestion actually reads, so the surviving copy was the one that mattered. Non-href segments now get that same generic pass; byte-safe because it only DELETES characters (never introduces a`"`) and because ADR 1020 has already VERIFIED the split parity, so an odd segment IS the value's exact extent rather than assumed to be. Found DOWNSTREAM in anyora's drain (ADR 0076) and paid back here | Accepted (HQ-ruled, ratify queued) | Implemented |
| [1023](1023-heatmap-data-url-keys.md) | **Skeleton-authored** (channel-A drain of `aa2c0c8`, by content; body verbatim, same voice caveat as 1022). Partly SUPERSEDED by [1025](1025-heatmap-merge-must-not-use-set-and-the-sdk-masks-nothing.md) — its `mask_personal_data_properties`sentence is false as an alternative to this scrub, and its merge loses a`**proto**`bucket.`$heatmap_data`'s object KEYS are URLs, and a key-reading walk is structurally blind to them. The heatmaps buffer is keyed by `location.href` and flushed as `capture("$$heatmap", { $heatmap_data: buffer })`; the SDK masks that href only under `mask_personal_data_properties`, which defaults to FALSE — so a raw `?search=<surname>`sat IN a key while every rule in the module read a key and rewrote a value. Enabled from the PostHog UI with no code change, so it can start flowing into a repo that shipped nothing new. Keys are now query-dropped and buckets MERGED — dropping the query collapses distinct keys onto one path and`Object.fromEntries`keeps only the last, so the naive build would make a redaction rule a silent DATA-LOSS rule. Found DOWNSTREAM in anyora's drain (ADR 0076) | Accepted (HQ-ruled, ratify queued) | Implemented |
| [1024](1024-parity-guard-must-check-the-final-segment.md) | **Perimetra-authored** (found by the adversarial review of this repo's own`aa2c0c8`drain — **owed upstream** to fullstack-skeleton and web-native-skeleton, which carry the same guard). Corrects [1020](1020-elements-chain-parity-must-be-verified.md): its parity loop was bounded at`parts.length - 1`, exempting the FINAL even segment as "trailing text" — and that is exactly where a split shifted by one bare quote parks the href VALUE. `x="y:attr**href="https://app.cz/p?token=SECRETTOK` splits to `['x=', 'y:attr**href=', 'https://…']`; only index 0 is checked, it ends in `=`and passes, and the URL sits unexamined at the exempt index 2 — read as structure, never scrubbed, chain returned BYTE-IDENTICAL. Fix: check EVERY even segment, requiring the last to be EMPTY rather than to end in`=`. The tightening is free — a well-formed chain ends on a closing quote, so that position is always `""`— and the odd-quote (truncated-chain) behaviour of [1018](1018-elements-chain-quote-alternation.md) is untouched, since an odd quote count puts the last index at an ODD position already covered. NOT a demonstrated live leak: the review could not build this shape from the real posthog-js serializer (both realistic tag_name-injection chains start with a segment not ending in`=`, so the existing loop already `[Filtered]` them) — fixed as defense-in-depth because [1022](1022-elements-chain-non-href-segments.md)'s soundness argument explicitly RESTS on the guard. Generalisation, distinct from 1020's producer-side lesson: **an exemption carved into a validator for a "can't matter" position is precisely where a shifted frame lands — a loop bound is part of the security argument, not an implementation detail.** | Accepted (HQ-ruled, ratify queued) | Implemented (`analytics-scrub.ts`full-range parity loop;`analytics-scrub.test.ts`shifted-split rejection + well-formed-chain acceptance, both disarm-verified) |
| [1025](1025-heatmap-merge-must-not-use-set-and-the-sdk-masks-nothing.md) | **Perimetra-authored** (same review — **owed upstream** to fullstack-skeleton, web-native-skeleton AND anyora-platform, all of which carry both defects verbatim). Corrects two claims and one implementation detail of [1023](1023-heatmap-data-url-keys.md). (1) The merge accumulated by ASSIGNMENT into an object literal, so a`**proto**`key hit`Object.prototype`'s setter instead of defining an own property: the bucket became the result's PROTOTYPE and vanished — **the exact data loss the merge exists to prevent**, through a door the ADR did not check, and a self-inflicted divergence from the `Object.fromEntries`idiom every other branch uses. Fixed by accumulating in a`Map`and finishing through`Object.fromEntries`(keeps the merge, drops the`[[Set]]`, still returns an ordinary object). (2) "Masked by the SDK only when `mask_personal_data_properties`is set, which defaults to false" states the default correctly but implies the flag would otherwise cover us — it does NOT:`heatmaps.js`passes only`PERSONAL_DATA_CAMPAIGN_PARAMS`into`maskQueryParams`, which rewrites just those NAMED params (`gclid`, `fbclid`, …), so `?search=Novakova`survives at ANY setting. The corrected claim is STRONGER, and the weaker one is dangerous in one direction — it licenses a derived repo to flip a flag INSTEAD of draining. (3) Records, without fixing, that`stripEmbeddedUrlQueries`preserves OUR parse but not the bytes we EMIT: deleting to end-of-segment can leave a trailing backslash adjacent to the rejoin delimiter, so`attr**src="https://a.cz/x\?q=1"` emits `…/x\"`; re-scrubbing that output is safe, and re-escaping on the way out is a larger hazard than the residue. Generalisation: **a comment asserting a security property needs a test pinning it, or it is prose that decays silently.** | Accepted (HQ-ruled, ratify queued) | Implemented (`analytics-scrub.ts` Map-based merge + corrected SDK and emitted-bytes comments; `analytics-scrub.test.ts` `**proto\_\_`-bucket + emitted-escape cases, disarm-verified) |

See [`../../ARCHITECTURE.md`](../../ARCHITECTURE.md) for the stack overview and
the consolidated constraints cheat-sheet.

ADRs 0001–0012 dated 2026-05-26; ADRs 0013–0014 (and the now-written 0009 / 0010)
dated 2026-06-01; ADR 0015 dated 2026-06-02; ADR 0016 dated 2026-06-03; ADRs
0017–0019 dated 2026-06-03; ADRs 0020–0028 dated 2026-06-04; ADRs 0029–0033, 0035,
0037–0038 and 0043 dated 2026-06-10; ADRs 0034, 0036, 0039–0042 and 0044 dated
2026-06-11. The numbering is complete — derived projects start at 0045 (see
[ADR 0042](0042-template-lifecycle.md)); skeleton ADRs written upstream AFTER
the stamp are renumbered into the project-owned range on drain, because their
upstream numbers are already taken here. Drained so far: upstream 0045 →
perimetra 0099 (authz default-deny), 0046 → 0104 (deployment tier), 0047 → 0105
(calendar date), 0048 → 0106 (pre-push test gate), 0049 → 0107 (app-level
tenancy). **Never land a drained ADR at its upstream number.** Cherry-pick does
not protect you: an upstream ADR whose basename differs from the project's ADR
of the same number adds a SECOND `00xx-*.md` file with no conflict at all.

**Exception — the reserved ≥1000 band ([ADR 1000](1000-adr-numbering-reserved-band.md)):**
since 2026-07-09 the skeleton numbers its own new ADRs at 1000 and above, and
will never again author one below 1000. Those ADRs drain into this repo
**verbatim, at their upstream number** — the renumber rule above governs only
the grandfathered upstream 0045–0049, whose numbers perimetra had already
claimed for unrelated decisions. Perimetra's own decisions continue in the
0001–0999 range (the next free number is 0121), so the two sequences can no
longer collide. The ≥1000 rows in the table above are the ones drained so far;
they are skeleton-owned, so supersede rather than edit them here.

Research against primary sources (npm, GitHub, official docs, create-t3-turbo
`main`) as of those dates.
